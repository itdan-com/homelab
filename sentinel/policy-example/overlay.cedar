// Hand-written Cedar escape hatch — validated together with the
// generated set at every activation. Expected to stay empty:
// anything accumulating here is a schema smell to fold back into
// the matrix (ADR-005 Decision 5).
