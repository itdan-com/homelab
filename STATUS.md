# Homelab Build Status

**Active phase:** **Phase 9 STARTING — both gating ADRs ACCEPTED 2026-08-23: ADR-010 (AWS shape — k3s-on-EC2, decision #13) and ADR-011 (Sentinel horizontal scaling, decision #14).** Phases 1-8 complete; Phase 7.8 external-identity arc closed (7.8.1 live-proven against real Okta, 7.8.2 documented, 7.8.3 EMA receiver built). Owner confirmed both in one decision round: *"Accept k3s-on-EC2, start Phase 9"* and *"Accept [ADR-011] and I'll push."* ADR-011 answers the "sentinel can't horizontal scale" challenge (N stateless brokers + shared Postgres + one isolated admin/kill/sealer, opt-in, SQLite stays default) — a bounded storage-swap, sequenced as the enterprise-tier deliverable with Phase 9's sizing profiles, NOT required to launch Phase 9. The Phase 9 build is no-account, `terraform validate`/`plan`-only until the owner's two-key billing-guardrail handoff (see AWS credential plan below).
**Status:** Platform green — 3 Sentinel units active, 0 unhealthy ArgoCD apps, 0 pods not Running, suite **152/152** (sentinel), working tree clean. **Phase 8 is now 5 of 6: ADR-006 BUILT 2026-08-22** (broker /metrics + audit copy to Loki through the repo's first mTLS TLSOption route — gate probed live, all five properties held; Airlock activity dashboard + 4 new alert rules incl. ADR-009 D5's sync-Unknown and the kill-switch page). The Prometheus half populates at the owner's next sentinel install; remaining Phase 8: the Slack webhook (owner-paced) and a Tempo go/no-go decision. **ADR-007 accepted 2026-08-15; Decision 1 (velocity) built same day** — protects Airlock's person/Cedar path; the classic flow-scoped MCP capability path (`check_capability`) does not yet have velocity protection — see STATUS backlog and the ADR's own "Decision 1 — BUILT" section before assuming full coverage. **Phase 8 is 4 of 6:** done — Loki+Alloy, Platform-health dashboard, LLM-usage dashboard with a values-driven price table, and 6 alert rules (writing them found TWO metrics nobody exported: argocd and cert-manager, now scraped). Remaining — the **Airlock activity view**, whose design is settled in `docs/adr/ADR-006-sentinel-observability.md` (Proposed, revised on owner challenge: BOTH a metrics endpoint on Sentinel's mTLS broker listener AND audit segments shipped to Loki over an mTLS-gated push-only ingress — because exporting the record to a separate system makes tampering harder, not easier); and **Alertmanager's Slack receiver**, which needs a webhook URL. Alerts currently fire into Alertmanager's own UI only.
**Next action — begin Phase 9 sub-session 9.1 (no cloud account needed): author `infra/` Terraform to `terraform validate`/`plan` fidelity** — VPC + subnets + k3s EC2 node + the trust-boundary modules (Sentinel VM, forge VM, their SGs/IAM, and per ADR-011 the shared-DB SG + DB-role definitions) written into the **agent-unreachable** location ADR-010 D4 requires, NOT the operator-PR-reachable repo. Then the ArgoCD GitOps-Bridge handoff (Terraform installs ArgoCD + seeds the root app; ArgoCD owns `catalog/`) and the catalog cloud-knobs (dynamic PVCs, HTTPRoute migration off Traefik, the four ADR-002 knobs). All simulatable with no account. Owner-paced Okta thread continues in parallel: send `docs/drafts/okta-xaa-ea-request.md` to developers@okta.com for the XAA EA flag, and revoke the dev-matrix API token if not yet done. Still trailing from Phase 8: the Slack webhook URL.

**AWS credential plan (owner-stated, holding until Phase 9 build starts):** owner creates Budgets + billing guardrails with a privileged key, then hands me a key that CANNOT touch billing; I author + `terraform validate`/`plan` everything with NO real apply until the owner is at the console with a confirmed cost ceiling. I never hold the key that can change spend limits. This waits on ADR-011 being settled (no point building Terraform on a Sentinel topology still under debate).

**ADR-008 (bring-your-own-IdP — Okta/Ping/Entra + the EMA receiver) ACCEPTED 2026-08-22** (owner, same day it was proposed: *"accept 7.8 i guess"*) — `docs/adr/ADR-008-bring-your-own-idp.md` is binding. **Phase 7.8 — Airlock: external identity** is green-lit, queued AFTER Phase 8's two remaining items; ADR-006 implementation is still the next action unless the owner pulls 7.8 forward.

**The GitHub demo is written down concretely** in `docs/demos/airlock-github-pr.md` — exact words to type, exact refusal text, what to click, how to verify, and a symptom→cause→fix table. Do not re-describe it in chat; point at the file.

**Before starting, run `scripts/sentinel-demo.sh`** if the console has never been seen doing anything — it raises a real capability request so a card appears, waits for the human to Grant, then completes the call through the proxy. That is also the demo GIF.
**Last updated:** 2026-08-23 (ADR-011 proposed, reviewed, and ACCEPTED same session — Sentinel horizontal scaling, decision #14; owner also confirmed ADR-010 k3s-on-EC2 + "start Phase 9" as decision #13. Phase 9 build begins at sub-session 9.1, no-account/plan-only until the owner's billing-guardrail key handoff. See top of Recent activity log.)

**GitHub remote:** https://github.com/itdan-com/homelab (**public** since 2026-07-26). Note: chat was verified from the **Windows-box browser**; Mac-direct access to `*.lab.local` still needs WSL2/Windows networking (backlog).

---

## Session protocol

Every new Claude session in this repo:

1. Read `CLAUDE.md` for architecture and principles.
2. Read this file (`STATUS.md`) for "where am I now."
3. Read `docs/phases/phase-NN-*.md` for the active phase's detailed checklist.
4. Execute work, ticking checklist items in the phase doc as completed.
5. Before ending: update this file's `Active phase`, `Status`, `Next action`, `Last updated` fields and prepend to `Recent activity log` below.

`CLAUDE.md` is the map. `STATUS.md` is the cursor. Phase docs are the territory.

---

## Recent activity log

_Append-only. Newest at top. One line per significant event. Date in ISO format._

- 2026-08-23 — **BOTH Phase 9 gating ADRs ACCEPTED in one owner decision round — Phase 9 is greenlit.** Owner accepted ADR-011 (decision #14, *"Accept and I'll push"* — Sentinel horizontal scaling settled: N stateless brokers + shared Postgres + one isolated admin/kill/sealer, opt-in) and confirmed ADR-010's k3s-on-EC2 substrate with *"Accept k3s-on-EC2, start Phase 9"* (decision #13; the ADR file was already ACCEPTED from the earlier *"your recommendation is fine"* — STATUS had lagged, now reconciled). **Next: Phase 9 sub-session 9.1** — `infra/` Terraform to validate/plan fidelity, no cloud account, trust-boundary modules on the agent-unreachable side. The owner's two-key billing-guardrail handoff (owner sets Budgets with the privileged key, hands over a no-billing key, no real apply until a confirmed ceiling at the console) gates only the *real* apply, not the authoring/simulation.
- 2026-08-23 — **ADR-011 PROPOSED — Sentinel CAN horizontal-scale, and the recon flipped my own "it can't" assumption before the ADR was even written.** Owner's challenge: *"i dont like that sentinel can't horizontal scale… you chose all this infrastructure and it can't even scale is what you're telling me? whats the best path forward?"* Four-agent recon graded the claim against the code and found the honest picture is three claims, only one a real gap: **throughput was never the wall** (`check_capability` is 2 indexed reads + 1 append; SQLite-WAL does thousands/sec, enterprise load is hundreds), **the real gap is single-instance HA** (a dead VM fails `ext_authz` closed), and **the fix is half-built** — the broker already holds NO authoritative in-memory state (the authoritative dicts live in the door/EMA layer), and the audit layer was built concurrent-writer + single-sealer from day one (Phase 7.6). So the shape is **N stateless brokers + shared Postgres + one isolated admin/kill/sealer**, opt-in with SQLite staying the lab default — a bounded storage-swap, not a rewrite. ADR-004's "one droplet is the honest unit" was a *complexity* claim (multi-writer state SQLite avoids), never a throughput bar — and ADR-010 already named this exact upgrade as future work. **Three-lens adversarial review (code-accuracy / trust-invariants / Postgres-correctness): 4 blocking + several minor, all folded in pre-commit.** The two that mattered most were the plausible-and-wrong kind: (1) my first sealer fix — a `ts < now()-5s` wall-clock settle window — was UNSOUND, because `ts` is a per-instance client clock (`default=utcnow` at flush), not commit order; under N broker clocks + commit-vs-flush lag a low-id row can commit after later rows were sealed and FORK the hash chain → false tamper alarm. Corrected to a transaction-visibility horizon (`insert_txid < pg_snapshot_xmin(pg_current_snapshot())`) chaining by a gapless sealer-assigned `seal_seq`, with a reverse-commit-order concurrency test that fails the old design and passes the new. (2) Dual-backend had SQLite as the *default* selected by env var — a dropped `SENTINEL_DATABASE_URL` would silently give each broker its own kill_state = **N diverging kill switches**, the one control that must never fail open, disabled by a typo → now a fail-loud `SENTINEL_TOPOLOGY=clustered` interlock (refuse to boot on sqlite://, hard error on missing URL). Plus: per-role DB least privilege so a compromised broker can't `UPDATE kill_state` at the storage layer (SQLite couldn't express this, Postgres can); the RDS's SG + DB-role Terraform moved onto ADR-010 D4's agent-unreachable side (else the operator agent could PR-widen the SG to the cluster — ADR-004 reason-1 reconstituted); transaction-scoped advisory lock (session-scope breaks under RDS-Proxy pooling); kill-read from the writer endpoint, fail-closed through the failover window; synchronous Multi-AZ standby for kill durability; L4-only LB (an ALB would strip the broker's sole mTLS gate); and the ADR-007 velocity rail named as best-effort-under-scale-out rather than silently weakened. Doc only, no code yet — the build is the enterprise-tier deliverable, sequenced with Phase 9's sizing profiles, lab-testable against a container Postgres with no cloud account. Awaiting owner accept/amend (decision #14).
- 2026-08-23 — **ADR-006 CLOSED END TO END — the gate's record now provably lives in two trust domains, and the closing proof was a denial read back from Loki with its hash chain.** After the httpx fix deployed (owner's third `--code` line), the 256-row backlog resolved honestly: rows older than Loki's 7-day ingestion window were 4xx-refused and loudly skipped (`skipped=256`, the designed behavior — canonical record intact in Sentinel, `SentinelAuditShippingSkipped` will show the one-time skip for ~an hour and self-resolve), and the LIVE path was then proven with a fresh row: one harmless `unknown-token` denial generated through the real broker → sealed on the 30s tick → shipped (`last_success` set) → **read back FROM LOKI** as `[denial/echo] id=257 flow=adr006-e2e` carrying its `row_hash` — the chain travels, the labels stay bounded, both dashboard halves are live. Phase 8 now stands at: Slack webhook (owner-paced) + Tempo go/no-go remaining. Next build item: the EMA receiver (7.8.3 second half — spike said GO).
- 2026-08-23 — **Decision #12 RESOLVED and the Okta live matrix fully staged — the owner has an org (`integrator-4949708.okta.com`), probed compatible the same hour.** Public-metadata probes: org AS + `default` custom AS live, RS256 id_tokens (the door's one alg), `client_secret_basic` (the door's default), userinfo present (the fallback path) — every 7.8.1 assumption confirmed against the real vendor. NOT yet XAA-enabled (no token-exchange grant advertised on either AS — ID-JAG minting waits on Okta's EA flag or rides xaa.dev). Two new no-root scripts stage everything: `okta-register-door.sh` registers the OIDC app IN the owner's org via a one-time revoke-after API token (their "you do it" standing order, honored — the wizard clicking is now mine), and `okta-live-matrix.sh` runs the matrix on the dev stack (headless half through the door's REAL discovery/JWKS code, then one sign-in URL — /link/github deliberately, the full browser federation leg with zero client tooling). The owner's irreducible acts: mint+paste the API token, type their Okta password once. Also settled in chat: the `--code` line stays a human act BY DESIGN (Sentinel is the trust anchor; Claude pushing code into it unilaterally would collapse ADR-004's separation) — but deploys get batched so it stays rare. En route: a bash trap worth its comment — an apostrophe inside a `${var:?message}` word opens a quote context even under double quotes; the matrix script carries the warning.
- 2026-08-24 — **ADR-010 PROPOSED — the Phase 9 AWS shape, and the review turned a wrong pillar into the right one.** Eight-agent recon (our ADR-002/004/009 + cost-shape + catalog blockers; AWS EKS/k3s economics, trust-VMs, terraform/GitOps, catalog swaps) then a three-lens adversarial review (4 blocking + ~16 applied). **The headline recommendation runs COUNTER to the owner's "aws k8s" instinct and says so:** k3s-on-EC2 as the default (~$110/mo vs EKS ~$236 — the entire premium is AWS plumbing/lock-in, and it's the same k3s the lab runs so parity is exact), with **EKS as a documented, unbuilt second module** for a customer who needs managed control plane. The owner OWNS this call — flip to EKS if it's for a real client/résumé and the ADR drops in either way. **Review's sharpest catches, all fixed:** (1) I'd built a pillar on a WRONG fact — "EKS Auto Mode can't serve Gateway API" — when the real disqualifier is the CNI lock (Auto Mode manages the data plane → no Cilium → breaks ADR-003); my own recon even cited AWS running Gateway API on Auto Mode. (2) Putting `infra/` in the operator-PR-reachable repo reconstitutes ADR-004 reason 1's self-approval hole — trust-boundary Terraform (SGs, SSM/IAM) now MUST live agent-unreachable. (3) SSM admin access is IAM-gated not physically-unreachable → explicit invariant: only the owner's human IAM principal may `ssm:StartSession` the Sentinel/forge VMs, every agent/cluster role denied. (4) I claimed "Envoy-terminated TLS" — the lab terminates at TRAEFIK; the Traefik→HTTPRoute door migration + a new Envoy TLS listener is the load-bearing edge work, its own sub-session. Also fixed: k3s floor corrected ($65→$110, a 2-vCPU node can't admit 2.2 vCPU of requests), trust-VM egress accounted, "unchanged catalog" → "after Decision-3 changes", single-node no-workload-HA stated plainly, operator VM sized up to t4g.small (2GiB, or it OOMs Mission Control), confidence/region tags on every number, and honest 5+-session scope. ADR-004 flipped to ACCEPTED alongside (it was Proposed-but-binding); cost-shape.md corrected for the missing 3rd (forge) VM. Awaiting owner accept/amend (decision #13), with the k3s-vs-EKS substrate call explicitly the owner's.
- 2026-08-24 — **Phase 8 CLOSED and 7.8.2 BUILT — the road to AWS is now just the Phase 9 ADR.** Phase 8: every dashboard item ticked or folded-with-reasons (App-metrics folded — the one trafficked service is covered, a RED dashboard waits for real Phase-9 users; Tempo deferred by written decision, revisit trigger = the first cross-service latency question logs can't answer), exit-criteria ledger closed (induced-anomaly proven to Alertmanager by the ADR-009 drill; disk 370G free of 1007G), one trailer = the owner's Slack webhook URL. 7.8.2 (Authentik broker mode): `docs/authentik-broker-mode.md` written from LIVE PROBES of the deployed 2026.5.6, not vendor docs — dedicated `okta`/`entraid`/`azuread` source presets confirmed present, `identifier` user- AND group-matching modes available (the takeover-safe pin), inbound SCIM is a real Source (`/sources/scim/`→200), and `force_authn` IS on the SAML source (the research had it "future/unverified" — the OPTIONS probe settled it). Bounds restated: broker mode does NOT fix Airlock's P1 (door-direct stays stronger for Airlock; broker is for the portal apps); concrete blueprints wait for a real customer (no dependency-before-it-exists). **7.8 arc: 7.8.1 live-closed, 7.8.2 documented, 7.8.3 receiver built — the whole external-identity phase is effectively done bar owner-paced Okta-side XAA enablement and the optional xaa.dev receiver run.**
- 2026-08-23/24 (the Okta evening) — **7.8.1's EXIT CRITERION CLOSED LIVE: the door federated a real Okta sign-in, and the matrix caught four vendor-truth bugs on the way.** The owner supplied a one-time API token (test org, revoke-after); with it: the door registered in their org as an OIDC web app (`okta-register-door.sh`), a fresh secret minted via the credentials API, and the org XAA-probed read-only (`okta-xaa-probe.sh`: `xaa-files` + `okta-mcp-inbound` custom AS both advertise **jwt-bearer — the RECEIVING grant our door now speaks** — but no token-exchange anywhere: the org cannot mint ID-JAGs until Okta flips the XAA EA flag; evidence packaged for developers@okta.com). Then the matrix (`okta-live-matrix.sh`, no-root dev stack): headless half passed instantly; the human half took FOUR attempts, each a real find committed with its test — (1) dev-stack's `${V:-}` defaults ate explicit-empty env and sent an Okta-pointed door's discovery to Authentik (the installer review's trap, second location); (2) Okta 401s Basic auth + client_id in the body, verbatim "Cannot supply multiple client credentials"; (3) the host measured 2s behind Okta and PyJWT refuses a future `iat` — the interactive path had no leeway (the EMA review fixed only ema.py; Authentik masked it by sharing the host clock); (4) a browser keep-alive held a pre-fix zombie door through four "restarts" (uvicorn graceful SIGTERM) — dev-stack `down` now kills by port ownership: "the pid file is a claim, the socket is the truth". **Final: `LIVE MATRIX: PASS` — principal `accounts@itdan.com` pinned to (`https://integrator-4949708.okta.com`, `00u15kppzlokHURlk698`), trail `credential_added` → `auth_success`.** ADR-008 D2 is now vendor-proven, not stub-proven. Owner reminded to revoke the API token.
- 2026-08-23 — **7.8.3 RECEIVER BUILT — Airlock's door now speaks the enterprise agent-auth standard, and the review turned a 500 into a paper trail.** `app/ema.py` + the door's `jwt-bearer` grant branch, off by default (`SENTINEL_EMA_ENABLED`): an enterprise IdP's ID-JAG redeems for the door's own person-token with zero consent screens — validated Keycloak-strict (typ `oauth-id-jag+jwt`, deployment-issuer signature, exact single audience, 300s lifetime cap, lock-guarded single-use jti, the redeeming client named in the assertion and proven via `private_key_jwt` against its CIMD keys — confidential-only per the draft's SHOULD) and joined ONLY through the interactive `(issuer, sub)` pin: no JIT ever, the assertion's email joins nothing, and **ADR-008 D5's hard rule is now a test** — an EMA token bounces off `/elevate` as cookie AND bearer. Two-lens adversarial review: 1 blocking (garbage client-assertion bytes escaped as an UNAUDITED 500 on a public endpoint — now a 400 with an AUTH_FAILURE row) + a probe-confirmed outage-in-waiting (PyJWT 2.13 refuses future `iat`; an IdP clock 3s ahead = every assertion refused — `leeway=10`), the success-before-refusal audit ordering, the jti TOCTOU, under-constrained client assertions (now: required jti/iat, capped, replay-checked), mixed-kty jwks crash, and the suite's first un-stubbed pass through the real JWKS kid-matching path. Accepted bounds named in the ADR: in-process jti cache = ≤300s replay window across a door restart; CIMD `jwks_uri` refused (inline keys only); static clients public-only for this grant. Suite 188→**196** (22 EMA tests). I also pre-fixed two bugs the review then confirmed (the ledger `.first()`-by-sub shadow; cimd.ClientError escaping as 500) — writing the review prompt found them first. Remaining for full close: the xaa.dev live run (one browser login, owner-paced, pairs with decision #12) and Claude-as-client (blocked on Anthropic's waitlisted beta, recorded). Deploys with the next `--code` install, inert until the EMA knob is set on a deployment whose IdP issues ID-JAGs.
- 2026-08-23 — **The Loki drain's last blocker was httpx itself — root-caused by refusing to trust three green probes.** After the hosts line landed the shipper's error moved to `TLSV13_ALERT_CERTIFICATE_REQUIRED` — while the installer's curl probe with the SAME cert passed the gate. Env file verified, deployed code verified, files verified; the divergence reproduced only when driving the shipper's exact client: **httpx 0.28.1 silently DROPS `cert=` when `verify` is a CA-bundle path** — the shipper presented nothing on every handshake while every curl-based probe (installer included) vouched for the wire. Fix: `_http()` builds an explicit `ssl.SSLContext` with `load_cert_chain`. The class is pinned by a NEW regression test that mints an ephemeral CA in-test and runs a REAL client-cert-requiring TLS server — no stubs on the TLS layer, deliberately, because this bug survived a three-lens adversarial review precisely because every test stubbed `_http()` and every live probe used curl. (Probe-the-live-path, one layer deeper: probe with the same LIBRARY, not just the same wire.) Suite 173→**174**. Deploy = the owner's next `--code` line; the 256-row backlog drains on the first tick after it.
- 2026-08-23 — **7.8.3 spike CLOSED (research half): the EMA receiver is a GO — and the ADR-006 Loki drain hit a new snag mid-verification.** Four-researcher pass on ADR-008's gating questions, all four answered: EMA is Stable and UNAFFECTED by the 2026-07-28 core (pure OAuth-layer extension, no revision pending — build against draft-ietf-oauth-identity-assertion-authz-grant-04); NO DCR anywhere on the path (CIMD + private_key_jwt is the unregistered-client story — the veto holds; note the draft's confidential-clients-only SHOULD); the receiver's validation chain is precisely specified and Keycloak's experimental receiver (read at source) supplies the reference discipline (typ `oauth-id-jag+jwt`, aud=our issuer, client_id==authenticated client, FORCED single-use jti, 300s assertion cap, audience-restricted minted token, NO refresh, no-JIT account mapping — which lands exactly on our (issuer,sub) pin); and **xaa.dev makes it testable with zero external accounts** (open playground, BYO-Resource mints real ID-JAGs audienced at OUR door, scriptable via RFC 8693 after one browser login) plus a ~50-line local test signer for the suite. **Honest boundary recorded in the ADR: Anthropic's EMA client is still waitlist-beta and Okta-wired, Claude Code has no EMA surface — Claude-as-client verification is blocked on Anthropic, not us; VS Code's policy-managed preview is the one self-driveable real client.** Findings written into ADR-008 as the "7.8.3 spike findings" section. Meanwhile the hosts line landed (verified in the Windows file + resolving in WSL) and the shipper's error MOVED: name-not-known → `TLSV13_ALERT_CERTIFICATE_REQUIRED` — the mTLS gate wants a client cert the admin process isn't presenting, while my curl with the SAME cert passes the gate (422). Env lines verified correct in the installer that ran; deployed code verified to have the knobs; root cause needs one owner grep of the root-owned env file + the next `--code` install (which also deploys 7.8.1 and, with the name now resolving, runs the loki wire probe that will print the truth).
- 2026-08-22 (late night) — **PHASE 7.8.1 BUILT — the door speaks to strangers, and the review saved the TOFU defense from my own migration window.** ADR-008 D1-D3 shipped in one session: **identity ledger** — the pin is now (issuer, sub) via migration `b6e4d1a8c3f2` (probe-proven with data both directions; TWO copy_from traps caught by inserting duplicates post-migration: column-flag constraints silently dropped, then carried-through constraints silently kept — the file's docstring now names both), plus `idp_stable_id` (Entra `oid@tid` — its sub is pairwise per app registration) and the audited, TTL'd, passkey-gated **IdP migration window** with per-person re-pin audit rows. **Door hardening** — nonce mandatory (no truthiness escape), azp hard-required on multi-audience, configurable `SENTINEL_OIDC_EMAIL_CLAIM` + userinfo fallback (OIDC-Core sub-match enforced; email TLS-attested on that path, stated honestly), Host pinning now conditional on the lab's split-horizon rewrite, `client_secret_basic` (spec default) with validated auth-method knob, external-IdP deployments default door tokens to 60m (D3's posture, previously unimplemented). **The two review-found defects fixed**: /link's infinite redirect loop (cookie door-wide, regression test asserts the server's real Set-Cookie path) and writerless `disabled_at` (console People-pane sign-in switch — and review caught that browser sessions needed killing too, not just bearers). **Installer**: preflight now door-shaped (any vendor's issuer), and every SENTINEL_OIDC_* knob resolves shell → previous-install → default, which closed two blockers (a `--code` redeploy silently dropped the client secret; external installs were IMPOSSIBLE — the unconditional HTTP_BASE/lab-CA defaults rewrote every IdP call to Authentik's address and pinned public IdPs to the lab CA). **The review's sharpest save: a same-issuer migration window is now REFUSED** — proven end-to-end that it would have turned the re-issued-mailbox TOFU defense into a 24h silent re-bind; also caught the (iss,sub) collision path 500ing AND rolling back its own audit row (now a clean audited `idp-sub-collision` refusal, both paths tested). Suite 152→**173**; three-lens review 4 blocking + ~15 applied; `docs/idp-registration.md` (Okta/Entra/Ping recipes + migration honesty: exact-issuer, first-writer-wins race) linked from SETUP. **Owner-paced to close 7.8.1's exit criterion: an Okta dev-org signup for the live matrix** (owner decisions waiting #12). Deploy rides the next `--code` install. Remaining: 7.8.2 broker docs+probes, 7.8.3 EMA spike.
- 2026-08-22 (night) — **ADR-009 BUILT AND DRILLED, same day as accepted — the GitHub-outage twin of the 6.5 lifeline drill, and every runbook number is observed, not assumed.** D2 tick surgery rehearsed against real failure shapes: blackholed GitHub → `verdict=github_unreachable` at exit 0 WITH the full envelope in the same log line (the watchman survives the outage — the original code died at the token mint before its own no-GitHub health check); 401 stub → `verdict=github_auth_refused` at exit 1 (App revocation is the owner-side kill switch and now FAILS the unit loudly, firing the new credential-free OnFailure marker — verified); all three GitHub call sites bounded; interactive mode degrades instead of locking the owner out; charter rule 6/7 + tick-prompt rule 4 carved consistently, `ACTION: local` vocabulary added. D3 mirror live as a user timer: HEAD-parity bare repo, clone-back-verified every run, 10 chart tgz + sops/helm-secrets cached with checksums, bootstrap tarball fallback, full `file://` clone demonstrated (initContainer image-vendoring deferred to the registry decision — named in the ADR's BUILT section). **D4 drill timeline (T0 = github.com blackholed in cluster DNS): T+69s hand edit REVERTED in <20s (selfHeal fights from warm caches — "pause first" is physics, not caution); first app Unknown T+6m37s, all 15 by T+11m05s, ZERO unhealthy throughout; D5 alert FIRING T+12m16s (the pager works); repo-server deleted mid-outage stuck Init:1/2 on the blocked wget ~5min (the ADR-006 finding, live); DNS restored → repo-server up in 44s → the ApplicationSet RE-STAMPED the per-app `automated.enabled=false` pause 2.5min in — the uncommitted edit lived ONLY behind the AppProject deny window, and died 20 SECONDS after the window lifted.** Runbook written into `docs/operator-cheatsheet.md` from those numbers; drill artifacts cleaned (coredns keys back to three, no windows, echo Synced/Healthy, timer resumed). ADR-009 closes built except D6 (forge — Phase 9 by design).
- 2026-08-22 (evening) — **ADR-006 DEPLOYED to the live units (owner ran `--code` install, 18:25) — the Prometheus half is LIVE end to end.** Broker /metrics answering through mTLS (policy version matches the installer's broker/door agreement check), Prometheus target `up` with the gauges in the TSDB, sealer current at 0 unsealed. The shipper is alive in its designed degraded state: 256 sealed rows queued, `last_success 0`, backlog gauge self-reporting — waiting only on the owner's `loki-push.lab.local` hosts line (installer printed the exact line; drains in one tick once it resolves). The install also landed migration `a3f7c9e21b04` on the live DB — velocity's tier column had been built and tested 2026-08-15 but never deployed to the units until now, which is worth knowing: the live gate ran WITHOUT velocity protection for a week after the build entry said "built." Also observed during install: the known inotify "directory watch" warning (sysctl still owner-pending).
- 2026-08-22 — **ADR-006 BUILT (Phase 8 item 4 + most of item 6) — the gate's record reaches the observability stack, and the review + probes carried the session.** The build: `sentinel/app/metrics.py` (hand-rendered /metrics on the mTLS broker — bounded labels PROVEN by test: a planted email and repo path never appear; windowed gauges because a pruned table cannot honestly back a counter) + `sentinel/app/loki_ship.py` (newly sealed rows pushed per 30s admin tick, each line byte-identical to the segment format so Loki's copy carries the hash chain; watermark-after-ack, seal cadence = retry policy, 4xx skips loudly) + the repo's FIRST Traefik TLSOption (`catalog/loki/templates/push-ingress.yaml`, own SNI hostname on purpose) + the off-cluster mTLS scrape job + the Airlock activity dashboard (9 panels: Prometheus rates beside Loki events) + 4 new alert rules (ADR-009 D5 sync-Unknown; kill-switch page — Phase 8 item 6's "every flip pages" is now real; metrics-absent; shipping stalled AND shipping-skipped, the adversary-shaped divergence) + two new PKI leaves with monitoring-namespace injections now run by BOOTSTRAP too (review-blocking: Prometheus hard-mounts the scrape secret — a rebuild without it would have bricked the whole metrics stack, KEDA's scaler signal included). **Three-lens adversarial review: 3 blocking + ~15 applied.** The other two blockers: `%g` rendered epoch gauges up to 83 min wrong (test's round number hid it — review proved it with 1755859312); and `prometheus-client` reaches EVERY broker route since the broker has no per-cert authz — **named as owner decision #11** rather than landed silently (it can file/poll capability REQUESTS from the monitoring namespace; it cannot grant — the passkey tap stays the only yes; narrowing option named: dedicated metrics listener + sub-CA). Also from review: rotation now 409s before pruning unshipped rows (pruning them would have silently MUTED the divergence alert), lazy-expired pending requests no longer paint the 3am row red forever, and the granted_via clamp accumulates instead of clobbering. **Acceptance probed on the live wire, all five properties observed**: valid cert 204 + line read back from Loki; certless refused AT THE HANDSHAKE; wrong-SNI + forged Host **421** (domain-fronting bypass closed); query API 404 through the push route; clientauth secret deleted live → fails CLOSED (valid certs refused too), recovers on restore. Suite 139→**152**. Mid-build find: **the Cilium IP-lottery recurred** (2 of 4 CiliumNodes stale post-reboot incl. a real address collision; 22 of 32 scrape targets blackholed, all green-looking) — fixed with the documented DS restart; and the residue exposed an OLDER breakage: kubelet (3 nodes) + apiserver scrapes down for the entire 3-day retention window, unnoticed because no rule watches those jobs. Backlogged. Awaiting: the owner's hosts line + install line (see Next action).
- 2026-08-22 — **ADR-009 ACCEPTED same day (owner: "accept and pushit") and the branch PUSHED — the public repo is current through both ADRs.** The hardening session is green-lit: tick surgery + charter carves, bare mirror + initContainer vendoring, the six-probe devlab pass then the runbook, and the sync-Unknown Alertmanager rule (buildable now, joins the six live rules). Queue as written: ADR-006 build → ADR-009 hardening → 7.8, reorderable at owner's word.
- 2026-08-22 — **ADR-009 PROPOSED (`docs/adr/ADR-009-github-outage-posture.md`) — the GitHub-SPOF assessment becomes a plan, and its adversarial review killed a credential before it shipped.** Owner's words driving it, committed here for provenance (the ADR's Status cites this entry): direction pre-agreed as *"yeah makes sense i tink github unreachable makes sense, break glass makes sense and slscak"*; on the mirror, *"you think a mirror is good?"* (answered yes — mirror as dumb insurance, standby forge rejected); the forge idea is the owner's own — *"im tempted so think a local github instance in the cloud with the stack could almost make sense and it auto pushing to the repo after"* — promoted by the ADR to a designed-but-deferred Phase 9 decision with the trust-domain constraints written down (own VM by default; branch-protection-as-code with a negative test, since Gitea makes GitHub's no-self-approval invariant droppable config); and the cloud end-state, *"a terraform script that just pushes this to like aws k8s i think"*, recorded against owner-decision #3 as the AWS lean. Six decisions: wait-it-out posture with fail-closed-for-the-agent (D1) · tick degrades instead of aborting, with `github_unreachable` (exit 0, logged) split from `github_auth_refused` (exit non-zero — App revocation is the owner-side kill switch and must stay LOUDER than outage noise, review-caught) (D2) · bare mirror + repo-server initContainer stops wget-ing sops from github.com at pod start (D3) · break-glass runbook AFTER the full six-probe devlab pass (D4) · the pager is an in-cluster Alertmanager rule on ArgoCD sync-Unknown — buildable now, joins the six live rules (D5) · forge-ships-with-the-stack designed, built in Phase 9 (D6). **Three-lens review: 4 blocking + ~14 more, all applied.** The blockers that mattered: the first draft put the Slack webhook in an OnFailure script on the operator host — a third long-lived credential in the agent's own trust domain, an ungated non-PR capability, and a way to spoof the alert channel (killed; the host handler is credential-free and the webhook stays in-cluster in Alertmanager); "degrade loudly" had NO firing mechanism (an exit-0 verdict never triggers OnFailure — the real pager is D5's rule); the forge-with-operator co-location option contradicted author≠approver three sentences after stating it (true cost now in print, fourth VM is the default); and the D4 probe list had dropped the deny-sync-window probe — the runbook's own recommended primitive was the unprobed one. Also corrected: no "root app" exists to pause (the ApplicationSet controller re-stamps child syncPolicy ungated by any per-app pause — the deny window is what survives recovery), the ghcr exposure is ~6 image repos not 3, and today's "operator host" IS Sentinel's host (STATUS #6 deviation) — named in the ADR instead of silent. Build: one hardening session after ADR-006, before or interleaved with 7.8, owner's choice. Awaiting formal accept (owner decisions waiting #10).
- 2026-08-22 — **GitHub-SPOF assessment COMPLETE — four separable breaks found, two of them not the obvious one.** The headline pair: the operator tick dies at the GitHub token mint BEFORE its own no-GitHub-needed envelope check ever runs (the platform loses its eyes exactly when ArgoCD starts diverging, with zero alerting), and the ArgoCD repo-server wgets its SOPS toolchain from github.com on every pod start (a mid-outage restart = ArgoCD renders nothing; a mid-outage REBUILD is impossible — 4 of 8 helm repos are GitHub Pages). Also nailed down with maintainer sources: selfHeal stops enforcing once apps go Unknown (break-glass kubectl sticks during the outage) but silently REVERTS uncommitted fixes at recovery — pause-sync-first / commit-before-reenable is the runbook's spine. Sentinel is fully independent (local policy git, no remote) — the trust-domain separation paid off unprompted. Full findings + P1-P4 recommendations + what NOT to build (Gitea PR standby, proposal queueing) in the backlog entry; scope as ADR-009 / one hardening session on owner green-light.
- 2026-08-22 — **ADR-008 ACCEPTED same day (owner: "accept 7.8 i guess? … gotta get started on some of this after we land").** Phase 7.8 green-lit, queued behind Phase 8's two remaining items (ADR-006 build; Slack webhook). Same session, still in flight at time of writing: a four-agent GitHub single-point-of-failure assessment for Mission Control (owner question: "what do we do about mission control's single point of failure when github goes down?") — ArgoCD-outage behavior, operator escalation paths, mirror/break-glass patterns; findings land below when complete.
- 2026-08-22 — **ADR-008 PROPOSED — bring-your-own-IdP (Okta/Ping/Entra) + the EMA receiver, from the owner's question: "connect to key identity players without being our own one stop shop — what would that take?"** Eight-agent research pass (four repo readers over the door/ADR-005/Authentik chart/landscape record; four web researchers over Authentik federation, the enterprise-SSO bar, the MCP identity ecosystem, and Okta/Entra/Ping specifics). **The core finding: P1 made the door ~90% IdP-agnostic by construction** — it consumes three identity claims (email/sub/name), zero authority from tokens, generic OIDC discovery/PKCE/JWKS — so BYO-IdP is a wart list, not a rework, and it doubles as the ADR-005 D2 named-bound fix ("an IdP outside the agent's writable domain") plus the market's now-table-stakes axis (ToolHive/agentgateway/Obot/MintMCP all take external OIDC; EMA/ID-JAG stable 2026-06-18, Okta issuing, Anthropic client-side). Three lanes: **7.8.1** genericize the door (installer preflight is Authentik-shaped at install-systemd.sh:394; email-claim mapping; conditional Host pinning; nonce; confidential-client provisioning; (issuer,sub) composite pin + audited migration re-pin; console disable-principal action) · **7.8.2** Authentik broker mode (Sources, identifier-matching only) for the portal apps · **7.8.3** EMA/ID-JAG receiver at the door's own AS (spike xaa.dev first; EMA additive NEVER replacing the interactive elevation leg — no machine-exchanged token may mint a door_session). One IdP per deployment; approvers/kill switch stay passkeys forever (deliberately hardening ADR-004's "convenience later" into never, recorded). **Three-lens adversarial review (repo-accuracy/principles/research-fidelity) found 2 blocking + ~19 fixes, all applied**: `disabled_at` has NO WRITER anywhere (the offboarding kill point is a check with no trigger — console disable action now a named 7.8.1 prerequisite); a Consequences bullet conflated the door's login-leg ID-JAG receiver with D10 rung 1's upstream-delegation ladder (rewritten: one of two blockers falls, upstreams accepting ID-JAGs stays open); plus a real pre-existing bug — **`/link` account-linking is an infinite redirect loop today** (door_session cookie is path='/elevate', so /link can never see a session; in 7.8.1's scope). Awaiting owner accept/amend; Phase 8 work unaffected.
- 2026-08-15 — **ADR-007 accepted; Decision 1 (velocity) built, tested, and adversarially reviewed same day — and the review is why this entry is long.** `context.actions_in_window` (`_1m`/`_1h`, generated from `config.VELOCITY_WINDOWS_MINUTES` so schema and query can't drift) is now computed by `service.actions_in_window()` from a new `AuditEvent.tier` column + composite index, and threaded into every context `ladder.decide()` builds — baseline, elevated, AND approved, computed once per call so a forbid referencing it fires at every rung the same way a tier-based forbid already does. Read Dogwood's actual grammar first (a general aggregation query language, not the shorthand keyword earlier research implied) and confirmed our own narrower, simpler design was the right scope. 9 tests (`tests/test_velocity.py`), proven order-independent both directions against `test_ladder.py` (a real cross-file bug found along the way: a minted grant with a real 30-minute window outlived its own test in the shared test DB and broke a LATER file's assumptions when run in reversed order — fixed by explicit revoke, not a blanket table wipe, after a blanket `DELETE FROM capability_grants` broke a foreign-key constraint against a DIFFERENT file's still-live rows). **Three-lens adversarial review (security/hot-path/Cedar-semantics) found six real issues, two fixed same session**: `is_authorized()` was never actually enforcing the Cedar schema's `required` flag at request time (only `validate_policies()` did, at activation) — a LATENT gap since ADR-005 D3's `resource.tier`, never triggered because tier is never actually missing in practice, closed by making `policy.SCHEMA` public and passing it to `is_authorized()` in `ladder._ask()`, the one place a live request gets evaluated; and zero test coverage distinguished the `_1m` from `_1h` windows (proven by mutation — collapsing both onto one 60-minute lookback left all 7 original tests green). Both regression-guarded, and the fix mutation-tested by hand (reverted, watched the new test fail, restored). **One finding NOT fixed, and it matters**: velocity only protects Airlock's person/Cedar-door path — `check_capability()`, the function behind the actual `/v1/ext-authz` Envoy callout that the classic pre-Airlock flow-scoped MCP capability mechanism still uses, evaluates no Cedar at all and has no velocity check. Assessed as low CURRENT exposure (Mission Control is PR-only and touches no MCP; the Airlock door's own per-message forwarding tokens flow through `ladder.decide()` first) but the path is live, documented, untouched, and has no principal/tier/policy-authoring concept to hang a fix on without its own design pass — recorded as a high-priority backlog item, not patched under time pressure. Two more findings (per-tool keying reads broader than it is; case-sensitive tier matching, pre-existing since Decision 3, now also a velocity-bucket evasion) documented as limitations rather than fixed, to keep the shipped diff reviewable. Full detail: `docs/adr/ADR-007-airlock-hardening.md`'s "Decision 1 — BUILT" section.
- 2026-08-15 — **MCP landscape research deepened twice on request: a real differentiators pass, then a nine-provider deep dive.** First pass (the original gateway/registry/personal/corporate survey) was fairly called out as description without a thesis — rebuilt the artifact's closing section into an explicit "what nobody else has" (6 items) vs. "what we'd steal" (5 items) split, headlined by a one-line pitch. Second pass, requested because "this is changing constantly": nine deep dives (AWS, Microsoft/Azure+EMA, Google Cloud, Cloudflare, Okta/Auth0, Salesforce, GitHub/Copilot, ServiceNow, the observability trio) checked against the same six axes Sentinel makes decisions on, not just "what exists." Landed two real findings folded into the backlog and into ADR-007 directly: AWS's Dogwood (Cedar-native temporal policy language, shipped six days after ADR-007 Decision 1 was written) as prior art read before building velocity; AWS's `PartiallyAuthorizeActions` as a second, more directly portable reference for the tool-visibility gap alongside Okta's. Artifact redeployed twice at the same URL.
- 2026-08-05 — **Sentinel console visual-hierarchy pass BUILT, verified, and deployed — the console's first UI change since it shipped, and owner-confirmed live.** `sentinel/app/console/{index.html,console.css,console.js}`: a one-line status summary next to the kill switch (pending count / live grants / policy-active — the same "resolved" instinct the People pane already used, applied to the whole page); the pending badge and its panel border turn urgent-red only when something is actually waiting, so a count meaning "3 decisions" no longer looks identical to a count meaning "50 audit rows"; Dashboard and Access split into a page-level nav instead of one flat five-panel grid, since Access already behaves like its own sub-application (five tabs, forms, history) in the code — the layout just hadn't caught up. **Verified before deploy, not after:** JS syntax-checked in a throwaway node container (this repo's own established technique), every JS `getElementById` cross-checked against the HTML, HTML tag-balance checked, suite still **130/130**, then driven end-to-end in a real headless browser against the repo's own no-root `scripts/dev-stack.sh` — screenshotted the urgent state, the Access view, a real Grant/Deny click working through the restructured DOM, and dark mode — before handing the owner exactly ONE command (`sudo scripts/install-systemd.sh --code`, which restarts both units and self-probes the live wire rather than trusting `systemctl status`). Owner ran it and confirmed sign-in. **Found and deliberately NOT touched, recorded so it isn't rediscovered expensively:** registering a NEW device/operator does not itself open a session server-side — `register/complete` never sets the session cookie, only `login/complete` does (`auth_routes.py`) — and `doRegister()` in `console.js` never calls a follow-up sign-in nor clears the `#enroll=` URL fragment, so a real second-device enrollment may bounce back to the enroll screen instead of landing on the dashboard. Pre-existing, unrelated to this change, out of scope for it — worth a look the next time the auth flow itself is touched.
- 2026-08-04 — **Pre-Phase-8-continuation audit: ADR-007 proposed — six-agent research audit of Airlock's authorization model against the 2026 industry state, run before resuming Phase 8.** Owner's premise (most MCP servers ship with no real authZ layer, long-lived keys, no human-in-the-loop) verified against real data (~25% of public MCP servers with no auth at all, ~53% on long-lived keys — Practical DevSecOps/MintMCP 2026 surveys; named incidents: Invariant Labs tool-poisoning, the Supabase "lethal trifecta," MCPTox's 72.8% attack success rate, OWASP MCP03:2025). **Two of three owner-raised scenarios turned out ALREADY SOLVED by what's built**: a support engineer's LLM inheriting broad OAuth access is closed by ADR-005 Decision 10's THE RULE (per-user credentials — the upstream enforces the human's own permissions, not the agent's); Snowflake-vs-Databricks resource granularity is closed by Decision 3's tier map. **Elicitation confirmed correctly NOT used for authorization** — MCP's own human-in-the-loop primitive is a server-authored, unsigned, unattested prompt (spec's own words: servers MUST NOT use it for sensitive decisions), and documented tool-poisoning research backs Sentinel's out-of-band-console design over it. **Four real, proportionate gaps found, each an extension of a mechanism already built, no new component**: velocity as a fourth Cedar context flag (`context.actions_in_window`, computed synchronously from Sentinel's own audit log — the "wipe 1 laptop vs 100" scenario, P1, highest priority for the owner's stated unattended-3am operating model); a live-attribute resolver on the resource map closing dynamic ownership scoping AND change-request gating with one mechanism (P2); lightweight access reconciliation in place of a full IGA vendor platform (P3); and approve-tier grant editing for partial/negotiated grants — "approve 4 of 6," not just accept/deny (P4, bundles with the console visual pass). Full writeup with sources and alternatives-considered: `docs/adr/ADR-007-airlock-hardening.md`. **Awaiting owner accept/amend** (owner has already indicated intent to build all four — see Owner decisions waiting #8). Console UX review requested same session; findings delivered in chat, not yet written up as a file.
- 2026-08-02 — **7.4 BUILT (bar the owner's 2-minute GitHub App) — and the credential model was REVERSED by verifying the vendor instead of the vendor's docs.** The chart: GitHub's official `github-mcp-server` image, unmodified, pinned `v1.8.0`, http mode, a Deployment behind the sentinel-proxy with no door of its own (NetworkPolicy admits only the proxy's Envoy pods) — it **ships with the stack**, and the owner's framing was right: *"github updates the repo, I pull it into our infra, it just works"*. **The reversal:** the owner asked whether a PAT means the whole workforce acts as them, and verification against the SHIPPED BINARY found http mode has **no static token at all** — no token field in its config struct, `GITHUB_PERSONAL_ACCESS_TOKEN` silently ignored, every unauthenticated request 401s. GitHub's own changelog claims a fallback that does not exist (upstream #2946), and my first chart was built on that false premise and would have failed every call. **Consequences, all upside:** the workload now holds NO credential (compromising the pod steals nothing); Sentinel's door injects one per call it just authorized; and this is GitHub's own architecture — their hosted server is the same codebase behind an auth proxy, which independently validates the shape. Their App-auth-is-stdio-only rule turns out to exist for our exact reason (a network-reachable server-wide app identity = ambient authority, PR #2797). **Security finding acted on:** `X-MCP-Toolsets`/`X-MCP-Readonly`/`X-MCP-Exclude-Tools` are read FROM THE REQUEST in http mode, so a caller could widen its own toolset — the door overwrites all three per forward, CLI flags stay the ceiling. **Then the owner refused the PAT** (*"we have to do this every 90 days for a production org? wtf?"*) and was right: `app/upstream_auth.py` now mints **GitHub App installation tokens** in-process (the same dance `gh-app-token.sh` already does for Mission Control) — cached, re-minted BEFORE expiry, GitHub's stated expiry authoritative rather than an assumed hour; the only long-lived secret is a private key that never expires, and nothing rotates by hand. Refusals are explicit (half-configured App, unreadable key, ambiguous installation) rather than guessing. Honest limitation recorded: one shared identity means GitHub attributes to the App, and no "on behalf of" header exists (#2201) — so **Sentinel's audit log is the sole record of which human acted**, and the named upgrade is `ghu_` user-to-server tokens, which the server already accepts, changing only where the door looks up the token. Suite 99→**109**. SETUP §1.6c rewritten for the App flow. Retracted honestly: a "not scalable for many users" caveat I raised — checked, it is isolation nuance not throughput, and GitHub runs this codebase hosted; replicas + KEDA is the answer.
- 2026-08-02 — **The door forwards THROUGH the enforcement point, and two undeclared paths got retired.** An allowed person-call now reaches a real MCP server the way in-cluster callers do: the door mints itself a one-call, 30-second, scope-locked token AFTER the ladder allowed, and `sentinel-proxy` independently re-checks the kill switch and derives scope from the request itself — zero human taps, and "nothing reaches an MCP server without a capability check" stays literally true instead of gaining an "…unless it came from the door" clause. **Found while wiring it:** the host-side path to the proxy was a **root `kubectl port-forward` running since 2026-07-28** — undeclared, reboot-fatal, invisible to `bootstrap.sh` — now a chart IngressRoute (`e26fa47`, live, correct 403 on an untokened call); and k3s Traefik refuses **ExternalName** *and* cross-namespace service refs by default, so the route sits in the controller namespace targeting the data-plane service via the chart's deterministic name helper rather than loosening two global flags. Live-only bug caught in rehearsal: an ORM principal crossed a closed session boundary (`DetachedInstanceError` on the first real call) — identity now travels as plain values. The final hop still shows `unknown-token` in a DEV run because a dev door writes its own SQLite while the installed broker reads `/var/lib/sentinel/sentinel.db`; both units share that file after the install. Old port-forward (PID 438042, root) can be killed once the install lands. Suite 96/96.
- 2026-08-02 — **7.3 BUILT — Airlock has a door, and the seven-approvals headline is dead.** Six items, one session: **7.3.1** both policy consumers now converge on disk truth (broker startup activation + a read-only store-signature watcher in both processes; torn writes and the save-vs-watch race closed; `policy_version` on the wire); **7.3.2** the door's OAuth client, static + public + PKCE, and the CIMD question answered by OBSERVING the real client against an instrumented AS — Claude Code only presents its URL client_id when the AS advertises the flag, which Authentik never will; **the owner's product frame then re-cut the phase** (*"we would be using authentik in an end state… im not sure why we can't use cimd but we are NOT using dcr its not secure"*, plus self-hosted MCP servers by default and a common-server catalog with Snowflake named) → ADR-005 D9 amended: shipped-Authentik IS the end state, so **the door carries its own minimal AS facade** and CIMD lands NOW; **7.3.3** the door itself — RFC 9728 + AS metadata advertising CIMD with **no registration endpoint ever**, SSRF-guarded metadata fetch, RFC 8252 loopback matching (which neutralizes claude-code #37747 by being correct), sign-in federated to Authentik, and the door's own RS256 tokens audience-bound to the resource (honoring the `resource` param Authentik ignores); **7.3.4** the door speaks MCP and the ladder decides every message — `visible_tools()` for policy-only visibility (no audit flood), a borrowable tool listed *and marked*, an unassigned server absent whole, no SSE so nothing outlives its policy; **7.3.5** the elevation doors — a refusal hands back a one-time link to a **browser page behind the IdP, deliberately not an MCP tool** (a tool is callable by the model, and a model that can elevate itself is the hole this exists to close), confirm self-issues, approve files a console card and `grant_request` now mints profile grants; **7.3.6** installer + certs + probes + docs + demo doc. **Measured end to end: handshake + list + three birthright calls = ZERO approvals; one write window = ONE act.** Three bugs caught by tests/rehearsal rather than production: naive-UTC timestamps stamping every token six hours in the future, arguments wrapped one level too deep (every mapped tool silently denying closed), and the issuer/transport split failing without its env. Suite 21→**96**. Awaiting: the owner's `sudo` install line + first sign-in.
- 2026-08-02 — **PHASE 7.2 CLOSED — the owner's verdict round: parent-group inheritance understood ("via" chips), write⊇read confirmed, never-allow's environment became a per-server dropdown (`96f9d19`, rides next deploy), and the spitball that sets 7.4's course: their OWN on-prem GitHub as the first real MCP server** ("allow access via our setup, then scope with cedar" — which is the three-layer design verbatim: instance-side credential bounds the server, toolsets bound the verbs, matrix/Cedar decides the people; two GitHubs = two servers = two matrix columns, so homelab-github stays `none` for everyone else). Owner also flagged Servers/Limits tabs as abstract against example data — correct, they are the onboarding surface for REAL servers; GUI v3+ iterates when 7.4 gives it something real. Owner's live matrix edits (all-employees×github read, engineering×github write) proved the GUI's save path on the production store. 7.2 exit: all six items done + live + owner-driven iteration loop demonstrated twice in one day. Next: 7.3 fresh.

- 2026-08-02 — **Access GUI v2 — the owner reviewed v1 live ("this gui sucks — I can't easily see who has access to what") and the rebuild followed the industry's answer.** Four lenses replacing the page-dump: **Groups** (click → members + per-server grants + `inherited via` chips), **People** (search, capped 20 for scale, click → *"can, right now (resolved)"* — effective level with provenance, computing the whichever-is-higher rule the owner had already intuited), **Servers** (leads with "who can reach it" bucketed by level; "environments" replaces tier/map jargon, plain sentence per server), **Limits & windows** ("Never allow — hard limits no window, approval, or grant can cross" as dropdown sentences; windows as add/remove chips with an explainer). Human labels for levels everywhere (*write — self-approve, timed / needs approval / always*). Patterns cited: GitHub repo access, Okta assignments, Tailscale ACL preview, AWS Access Analyzer — the shared insight: show RESOLVED access, never make the human simulate policy. Same draft→structured gate underneath; 46/46; JS node-checked (`6cfa658`). Iteration continues on owner review.

- 2026-08-02 — **7.2.6 BUILT — the matrix is clickable, and the raw layer lives on underneath.** Owner's first look at the Access screen vetoed raw YAML as the daily surface ("an actual GUI… Groups, permissions, tools — something that really makes sense") — second correct friction veto of the day. Built in-session: level dropdown per group×server cell, people rows with group checkboxes, parent-dropdown groups (`all-employees` locked — load-bearing), forbids as dropdown sentences, in-place server tool-class editing (new servers start with `rpc.*` read so they can handshake). New `PUT /v1/policy/store/structured`: JSON in → server-side deterministic YAML (unchanged intent = unchanged version, tested) → the SAME validate→activate gate; **overlay preserved from disk on GUI saves** (the escape hatch can never be silently blanked — tested); rejection leaves store + draft standing (tested). Advanced drawer keeps the raw documents (owner's ask verbatim), labeled that GUI saves strip comments. Suite **46/46**, JS node-checked. Residual: tier/resource-map guided form = future polish (Advanced covers it). Cert saga same hour: `ERR_CERT_AUTHORITY_INVALID` on first https visit → the 5.5.7 certutil auto-trust had **silently skipped under sudo** (no interop in root's PATH), masked by the http era; CA trusted by hand for the Windows user, installer now falls back to the full interop path and warns loudly (`3cc4eac`). Awaiting: deploy line + owner's GUI verdict → 7.2 closes.

- 2026-08-02 — **7.2 LIVE — and the new battery's first live assertion caught a five-day-old transport regression from 5.5.7.** First install run: everything green until the smoke's first https call died `SSL: WRONG_VERSION_NUMBER` — diagnosis: **the admin console had been serving plain HTTP on loopback since 2026-07-27**, because `systemctl enable --now` starts a stopped unit and silently leaves a running one on OLD code: 5.5.7's same-evening https fix to the unit file was written, daemon-reloaded… and never restarted into. Five days of `Active: running` were true and misleading at once (bounded: loopback-only bind, nothing off-box could reach it; passkey unaffected — WebAuthn binds RP ID, not scheme; the old battery adapted to the env's scheme instead of asserting production's, which is how 5.5.8 passed against it). **Fix `b8e0717`: a deploy RESTARTS both units, then probes the WIRE** — strict-TLS console healthz against Sentinel's own CA + mTLS broker healthz — or fails loudly on the spot. Re-run: `wire probes: admin https 200, broker mTLS 200`, units fresh, migrations applied on live (snapshot kept), **policy store ACTIVE `bb04174fb4b8`** (and the seed-once property proven live: second run touched nothing), battery **18/18** including the full 5.5 enforcement chain. The headline "one MCP session required 7 human approvals" is the OLD path measured on purpose — the before-number 7.3's confirm door exists to kill. Remaining for 7.2 close: owner's first look at the Access screen. Backlog: WSL `Failed to allocate directory watch` cosmetic during systemctl (inotify limits; harmless).

- 2026-08-02 — **7.2.5 builder half DONE — the installer knows about the policy store, and re-installs greet an enrolled human correctly.** Installer deltas: hard **git requirement** (policy history is a local git repo — refuse at install, not fail quietly at runtime), **pre-migration DB snapshot** kept ×3 (the hosts-file lesson written into the deploy path), policy store **seeded once** from `policy-example/` and never re-seeded (`SENTINEL_POLICY_DIR` joins the env file; unit needs no change — `EnvironmentFile` carries it), and an operators-exist branch: re-install prints **"log in as usual"** instead of minting an enrollment code at someone who already has a passkey. Smoke battery asserts the policy plane before anything else (status ACTIVE + grants surface). Honesty: the end-to-end one-approval demo moves to 7.3 (it needs the confirm DOOR; unit-level retirement already proven). For-7.3 note recorded: console activation lives in the ADMIN process — the broker needs its own activation + reload when the ladder gets a route. Suite 45/45; installer `bash -n` + smoke `py_compile` clean. Remaining: owner's one line + first look.

- 2026-08-02 — **7.2.4 DONE — the Access screen exists: the matrix is rendered, edits validate-then-activate, restore is a button, revoke is a button.** New `policy_change` audit type (migration `f41d02a9c8b3`, fourth hand-widened CHECK; **rejected saves audited on purpose** — a stream of rejections is somebody probing the policy surface). Store API: GET (disk texts + parsed view), **PUT with the load-bearing property that a rejected save never touches disk** (candidate validated in a throwaway dir; tested — broken save → 422 with every error listed, active version unchanged, disk unchanged), GET history from the store's git, POST revert (**forward restore**: old sources re-activated, content-hash yields the SAME version id as a NEW commit — tested: 3 commits after restore, nothing rewritten). Console: matrix grid + people rendered read-only, four textarea editors (never repopulated by the poll — unsaved edits are the operator's), Save & Activate with full error display, history Restore buttons, "Borrowed right now" panel with per-grant Revoke. Suite **45/45**; JS syntax checked via throwaway node container. Deferred as polish: clickable dropdown grid (textareas + rendered view is the MVP). **Nobody has SEEN the screen yet** — browser look rides 7.2.5's install. Next: 7.2.5.

- 2026-08-02 — **7.2.3 DONE — the ladder runs in the broker, and forbid beats a grant you're holding.** `app/ladder.py::decide` for person-calls: kill first → no-active-policy denies closed → classify via store (`unclassified-tool`: the platform does not guess whether an unknown verb is dangerous) → derive resource (reject-don't-escape; `unmapped-resource` on missing/injection-shaped args) → baseline/elevated/approved Cedar asks give the OUTCOME CLASS → then POSSESSION: any live covering grant satisfies `elevated`, only human-issued (`granted_via` approve|admin — the console door) satisfies `approved`; a borrowable denial carries the offer (`profile` + windows from the matrix cell via transitive groups). `_grant_covers` gained `.*` prefix classes (handshake rides one snapshot entry; the dot is load-bearing — `github.rpcx` doesn't match). Every decision row stamps principal/resource/`policy_version`. **Proven (42/42, 9 new):** handshake-at-zero-approvals (the six-approvals finding retired in miniature — assigned server reachable with no grant, no tap); confirm offer→mint→yes→revoke→offer-returns (possession is live-checked); self-issued grant does NOT unlock the approve rung; **prod-tier write stays forbidden against a caller HOLDING a valid human-issued grant** (the owner's SoT stance enforced against possession, not just absence); unassigned server invisible even to `initialize`; store-unknown person = forbid regardless of DB rows; kill beats policy. No HTTP route yet — 7.3's door wires `decide()` to the gateway, by design. Next: 7.2.4.

- 2026-08-02 — **7.2.2 DONE — the policy store exists, and the ADR's proof now runs against GENERATED policy.** `app/policy.py` (cedarpy 4.8.7 pinned): three documents → semantic validation reporting ALL errors at once (a console save shows the whole story) with a **Cedar-literal injection guard** (emails/groups/servers on a safe charset, rejected never escaped — escaping is where injection bugs live) → deterministic matrix→Cedar generation (write* implies read; `write-on-request`→`context.elevated`, `write-on-approval`→`context.approved`; forbids are engine-level) → `validate_policies` against a schema requiring `Resource.tier` (the skip-on-error defense made checkable) → **atomic activation, last-good-stays-live** (broken edit raises, old version keeps serving — tested) → content-hash version → **auto-commit to the store's own local git** (author = console actor; unchanged store = same version, no commit). `policy-example/` is committed documentation AND the test fixture — they cannot drift. Admin `GET /v1/policy/status`; startup activation best-effort (inactive store = person-path denies closed). **Suite 33/33** (12 new); crown assert: the full ladder against generated policy — alice permit/confirm on github, implicit all-employees birthright, hr can't-even-ask, **hr-head `approve` on hr-staging while hr-prod write stays `forbid` through every context** (the owner's source-of-truth stance as physics). Prod install gains a git dependency (7.2.5 note). Next: 7.2.3.

- 2026-08-02 — **ADR-005 ACCEPTED (owner) + 7.2.1 DONE — the person arrives in the schema.** Migration `86c82f996509` (autogen inspected; two hand-fixes: `server_default='admin'` on the NOT-NULL `granted_via` — bare autogen fails on any table with rows — and NAMED FKs because `drop_constraint(None)` raises at downgrade; proven up→down→up). Landed: **`principals`** identity ledger (email-keyed; **TOFU idp_sub pin** — same email + different sub = refused + `idp-sub-mismatch` audit row; `disabled_at` offboarding), `principal_id` on flows/requests/grants (**ADR-004 debt 1 attribution closed** — the "two users' flow-1 collide" half retired by design, not by re-keying: person-flow ids are gateway-minted in 7.3; recorded in phase notes), **profile grants** (`tools_json` SNAPSHOT at mint so policy edits never widen live grants; `granted_via` admin|confirm|approve = the carve visible per row; `flow_id` nullable, and **flow-less grants deny closed** on today's path until 7.3's person-door exists — tested), audit `principal`/`resource`/`policy_version` columns (enum untouched — no CHECK rebuild this time), and **per-grant + per-flow revoke** (ADR-004 debt 4: `mint_profile_grant`/`revoke_grant`/`revoke_flow` in service + `/v1/grants` list + revoke endpoints on the admin listener, 409 double-revoke, zero-swept = success). **Suite 21/21** (13 pre-existing green, 8 new: TOFU/mismatch, snapshot coverage + flow-lock, deny-closed placeholder, USE rows carry principal+profile, mid-window revoke, flow sweep, API pass with sign-in). Deferred, recorded: elevation-close audit event (expiry is lazy; decide at 7.2.4). Live units untouched — rollout is 7.2.5's install. Next: 7.2.2 policy store + generator.

- 2026-08-02 — **ADR-005 second refinement: the matrix gains its high-risk rung, and it turns out to be already built.** Owner extended the ladder — read · write · write+temp-approved · **write+manual-approve (high risk)**, everything audited, "maybe in sentinel that's where that would live?" — and yes: `write-on-approval` IS 5.5.5's pending-card + Grant/Deny flow reused as Airlock's `approve` path. Same windowed-grant primitive as `confirm`, entered through the other door: the request lands as a console card (person, profile, window, reason), a passkey holder grants, `granted_by` names the approver instead of the requester. One primitive, two doors — the CLAUDE.md line "Sentinel's token is the elevation primitive both flows build on" turns out literally true. Matrix now five levels; generator mapping stated (read/write→baseline permits, write-on-request→`context.elevated`, write-on-approval→`context.approved`, none→nothing, forbid trumps all rungs); lab collapse named honestly (one enrolled human = requester≈approver until a second passkey enrolls — mechanism still enforces console grant + window + audit). ADR status: amended twice same day on owner review.

- 2026-08-02 — **ADR-005 amended on first owner read — the PR-authored policy repo died in review, which is the review working.** Owner: no "hyper manual flow for PRs to approve cedar context" — wants admin-configured groups/permissions/birthright in a GUI, everything audited, simple enough that automation could draft changes. The first draft had quietly broken CLAUDE.md's "owner never types git": it made the human the PR *author* for every onboarding and permission change. Decision 5 rewritten — **Sentinel owns the policy store; the console is the pen; git is the memory**: a new Access screen (7.2) edits the group×server matrix (`none/read/write/write-on-request` + windows + tier rules — the owner's sentence is the schema), Cedar is *generated* from it (templates; raw-Cedar overlay for edge cases through the same validation gate), save = validate→activate→`policy_version`→audit diff, and Sentinel auto-commits each version to a local history so revert is a console tap. P1 intact and simpler (the second-GitHub-repo surface is deleted); consciously given up: second-human pre-review — mitigated by audit+history now, Phase 8 alert on `policy_version`, two-passkey confirm named as the multi-admin future. The automation idea got its safe shape: **anything may draft; only a passkey holder activates** — a model that could apply policy is the self-granting hole, a model that drafts is free labor. `homelab-policy` repo cancelled from Consequences/Next-action. Commit `e88a03f` + this amendment.

- 2026-08-02 — **7.1 DONE — ADR-005 Proposed: Airlock has a policy model, and the sharpest finding made the design.** Owner supplied the elevation model in their own words at kickoff (groups → birthright; requests auto-approved *because* the group entitles them, windowed 30m/1h/2h; unassigned server → can't even request; sub-groups per role; prefer forbid over approve on sources of truth; access-request workflow = non-goal) — encoded verbatim as the ADR's opening. **The finding:** audit blocker 3 ("group membership is PR-able") was understated — the blueprints carry the **claim-computing expressions themselves**, so a routine SSO PR could make the IdP assert any group for anyone → **P1: authorization never derives from agent-writable state** → blockers 3 AND 4 collapse into one answer: a second **policy repo** (operator App not installed) carrying Cedar policies + the entity store; in-cluster Authentik authenticates only, and the elevation path's assurance bound (cluster-admin can impersonate) is named and accepted for the lab with the external-IdP fix stated. **Proven before claimed:** `cedarpy` 4.8.7 wheel (cp312, no Rust) installed on this host; four-outcome ladder 5/5 — permit / confirm-via-context / **forbid through elevation** (engine-level forbid-trumps-permit: "no button" is physics) / HR's double negative ×2. Cedar trap caught for 7.2: evaluation property 3 is **skip-on-error** — a forbid referencing a missing attribute is silently skipped, so `tier` is total by schema or a data bug becomes a permit. Three sonnet research spikes (URL-cited): **ToolHive v0.41.0 embeds Cedar** (official cedar-go) as its default MCP authorizer — the industry converged on our layer 3 — and verifiably has **no** human-in-the-loop grant/TTL/elevation at any tier, so the Sentinel primitive stays ours; **Authentik 2026.5.6** (= current upstream) is door-sufficient (OAuth 2.1/PKCE/RFC 8414) with zero RFC 8693/ID-JAG — and **DCR got deprecated** by the 2026-07-28 MCP spec anyway (CIMD + static registration won), so the 7.3 door needs nothing Authentik lacks; **XAA is real but elsewhere** (SEP-990 Final; Okta ships, Entra issues, Anthropic's client side beta/waitlisted) → layer 1 stays scoped static creds, upgrade path named. Forward decisions banked: **7.4** = official `github-mcp-server` native **http** mode (App auth stdio-only; no server-side repo allowlist → fine-grained PAT is the lever; negative-test `--read-only`, it had a real pre-1.0 http bypass); **7.5** = korotovsky + `xoxb`, never stealth tokens (official `mcp.slack.com` is hosted-only SaaS → bypasses the gate); **Socket Mode verified unnecessary** for 7.5's tools (outbound Web API only) — phase-doc section corrected with the trigger condition. ADR-004 amended (the carve: approvers = passkeys; self-elevators = IdP). Next: owner accepts/amends ADR-005 → 7.2 fresh session (create `homelab-policy`, tenant scoping first).

- 2026-08-02 — **PHASE 6 CLOSED — Mission Control complete, and the demo was the operator refusing to be fooled three times.** The plan said "induce load, get a PR, merge it on camera." Reality: burst 1 (9b, KEDA 1→3 in ~90s) → decline ("fresh ~2min burst, per-replica 23.3/30 under threshold"); bursts back-to-back (~11 min at ceiling, both tripwires firing) → decline ("bursty, not sustained-climbing; zero errors/restarts/throttling"); then the honest escalation — **`qwen3.5:4b` added to the gateway** (the 9b tops out BELOW KEDA's 90/s aggregate target, so a ceiling raise could never be justified with it; the 4b sustained 97–110 tok/s, per-replica OVER target at ceiling) → the sharpest decline yet: *the agent ran a Prometheus RANGE query, recognized the load-test's on/off square wave, cross-checked the scale cycle in the event log, and named the scale-demo tool as the cause.* Three wakes, $1.23, zero false proposals — **the judgment is the demo**, because the proposal machinery was already proven (PR #7, issues #6/#8) and an operator that proposes on noise trains its human to rubber-stamp. Record with verbatim verdicts: `docs/demos/mission-control-three-declines.md` (also carries the tripwire + guards tables); exit criterion amended to say what was actually proven. Banked en route: 4b as the local bulk/canary tier (~2× the 9b, warm-tested through the production path), `nomic-embed-text` pulled for the vector phase, the burst's MODEL parameterization, the k6-key rebuild-ephemerality finding, and the cheatsheet's three fossil sections corrected (Slack-approvals "Phase 6+", the never-built `claude-control` pod, "5.5 will give you a kill switch"). Next: Phase 7.1 (ADR-005), fresh session.

- 2026-08-01 — **6.5 DONE — the lifeline holds, and the drill caught the agent healing itself.** Dead-port kubeconfig variant (zero cluster impact; timer paused for determinism): the tick survived, flagged `api_unreachable`, and **filed issue #8 through GitHub while its cluster view was dead** — the exact property Mission Control's outside-the-cluster placement exists to buy. The debrief was richer than the pass ($1.58, 38 turns): the agent initially blamed a plausible-but-wrong `bootstrap.sh` corruption story, **caught its own misframe after reading STATUS.md mid-pass**, tried to post a correction, and found the App token 403s on issue comments — so "the agent updates the issue on recovery" is structurally impossible today (tracked for 7.4's App-scope work; human-closes stays the design: the agent reports, humans decide). Buried in the wrong story was a REAL latent bug the drill would never have found on purpose: bootstrap step 10 mints the operator kubeconfig from the *ambient* kubectl context — a bootstrap run pointed elsewhere silently writes wrong credentials (#8 stays open to track the named-cluster fix). **The boundary finding: the agent restored its own kubeconfig from the drill's `.bak`** — careful (server line only, token/CA untouched, forensics copy kept) and exactly the self-healing that inverts control in a real quarantine → **charter hard rule 7**: your own access is not yours to repair; a broken eye is a report, not a repair. Recovery tick green, timer resumed, drill artifacts removed. Next: 6.6, the demo (owner in the loop).

- 2026-08-01 — **6.3 + 6.4 DONE — the credentials exception is stated and the bypass decision is made by the owner, with the gate re-proven live.** 6.3: operator charter gains "Your own credentials — the stated exception" (Anthropic key to think, App key to open PRs — what makes the agent exist, not what it does; Sentinel cannot gate the call that would ask it; blast radius bounded by view-RBAC + PR-only App perms); config dir 755→700 (files were 0600); SETUP §1.8 gains the dedicated-key recipe (workspace + spend limit + one env line, edited not echoed) and rotation paths; agent log lines now carry `auth=api-key|login` so the key switch is observable — the mint itself is the phase's one owner-console action (STATUS owner list #5). 6.4: owner decision **keep the admin bypass** — *"that's for at the end, then we remove ourselves"* — because the builder commits as the repo owner and GitHub refuses self-approval, so removal without a second approving identity deadlocks all direct work while adding nothing to the operator gate (App PRs need a human either way); recorded adopter-facing in SETUP §1.2; the old "Phase 6 removes the bypass" plan is retired so no future session "finishes" it. **Gate re-proven on PR #7** (App-authored one-line doc PR, made for this purpose): App self-approve → 422, App unreviewed merge → 405, owner approved + squash-merged as themselves → OK. Clarity note that prompted the re-ask: Mission Control's PR path was never in question — the bypass concerns only the BUILDER's direct pushes, a third path belonging to neither flow. Next: 6.5 lifeline drill.

- 2026-08-01 — **6.2 DONE — Mission Control ticks by itself, and the operator's first live passes were charter-perfect.** The shape: a 5-min systemd USER timer → `launch.sh --tick` → deterministic `envelope-check.sh` (api/nodes/pods/argocd/keda/tokens/doors) → green = one heartbeat line, ZERO tokens; findings = one headless sonnet pass, guarded in script BEFORE any spend (3-open-PR cap counted from GitHub truth, 60-min per-finding cooldown, 20 passes/day, 15-min timeout, hard-reset workspace = PR-or-nothing), cost+tokens per pass logged to `observations.log`, raw result kept in `last-result.json`. Units in `ops/operator/deploy/` + `install-tick.sh` (detect-at-install; committed units host-agnostic per ADR-004); SETUP.md §1.8. **Two bugs found by running, both fixed:** (1) `view` RBAC cannot list nodes and the first envelope run printed `0/4 Ready` as *ok* — now fewer-nodes-than-expected is a named anomaly AND `operator-node-reader` grants get/list/watch; (2) the readout piped JSON into `python3 - <<heredoc`, whose heredoc replaces the pipe as stdin — three PARSE-ERROR ticks while the passes had actually succeeded; now parsed from the forensics file (plus `</dev/null` on the claude call to skip a 3s stdin sniff). **The agent proved the design live:** pass 1 (forced wake, green platform) concluded `ACTION: none`, noticed the harness PARSE-ERROR in its own observation log, correctly ruled it out of tick scope, and stated a recurrence threshold; pass 2 saw the recurrence and filed issue #6 — the charter's escalation path used correctly on a real defect — closed same hour citing fix `9545edf`. Measured pass cost $0.48–0.78; quiet ticks cost nothing. Next: 6.3.

- 2026-07-31 — **6.1 DONE — the operator can finally see: Prometheus through the API-server proxy, scoped to one service.** `k3d/operator-view-rbac.yaml` gains Role+RoleBinding `operator-prometheus-proxy`: verb `get` on `services/proxy` in `monitoring`, resourceNames pinning every URL spelling of the Prometheus service (proxy authorization carries the name exactly as written in the path). Battery as the operator identity: `up` → 26 series; the scrape-targets API answers through the same path (extproc :1064 target up); same-namespace alertmanager proxy **403** (resourceNames scoping proven, not just namespace scoping); cross-namespace argocd proxy **403**; create/exec/delete/secrets all still `no`. **Signal verified live, not assumed:** the token counters did not exist (zero series — nobody had generated since the rebuild; counters are born on first increment), so one 120-token completion went through the production path — two series appeared on the next scrape, and KEDA's exact ScaledObject query (`gen_ai_token_type="output"`) went from empty vector to computing values. Charter rewritten: proxy path + three canned queries replace the `kubectl exec` instruction that `view` RBAC had silently made impossible; "empty vector ≠ zero" noted for the future tick prompt. Next: 6.2, the continuous tick.

- 2026-07-31 — **The phase docs now say what the 2026-07-28 decisions said — and the operator turns out to have had NO working metrics path at all.** Session-start orientation found the territory two re-cuts behind the map: `phase-06-control-plane-claude.md` still ordered the cancelled phase (in-cluster agent, Slack ✅, MCP catalog, Sentinel gating) and STATUS's own Next-action paragraph contradicted itself (head: no Slack/no MCP/ADR-005→7.1; tail: "control-plane Claude in `platform-control` with Slack approvals"). Re-cut structurally, nothing deleted: **`phase-06-mission-control.md`** (renamed) carries the real Phase 6 — 6.1 Prometheus read path → 6.2 continuous tick → 6.3 credentials exception → 6.4 admin-bypass decision → 6.5 lifeline drill → 6.6 demo+close, exit criteria to match; **`phase-07-airlock.md`** created as the collection point (five sessions 7.1–7.5, the four surviving ADR-005 blockers, the SSE/tools-list/RP-ID/DCR gaps, Socket Mode, the XAA+Cedar write-up); observability and cloud docs renumbered to 8 and 9 (cloud doc also gains its three provider-independent blockers); CLAUDE.md's five stale spots fixed (profiles are 7.2 not "Phase 6's first work item"; "Phase 6 leaves a catalog of MCP servers" — it leaves none; two 6.5s; the Phase-8/9 test and ordering). **Found while making 6.1 factual:** the operator charter's documented `kubectl exec` metrics fallback was never executable (`view` RBAC denies `pods/exec`), and the API-server `services/proxy` path answers PromQL under admin — one scoped Role away for the operator. Probe gotcha kept in the doc: `auth can-i get services/proxy` parses `proxy` as a service NAME and false-positives; use `--subresource`. **Newly named, owner call pending (phase-06 open question 1):** the operator runs on the WSL2 host today — Sentinel's host — which CLAUDE.md property 2 forbids; mitigations exist (granting needs a WebAuthn session; Sentinel state owned by `sentinel`), decision is named-interim-deviation vs move. **6.4 wrinkle recorded:** the BUILDER's own pushes to `main` ride the admin bypass, so "remove the bypass" deadlocks the daily workflow without a second identity — honest options are in the doc.

- 2026-07-28 — **Flows untangled, phases re-cut, and the owner found a real isolation hole.** Owner corrected a mix-up of mine: **GitHub is Mission Control's substrate; Slack belongs to Airlock** — and a *GitHub MCP server* is Airlock too (all users, birthright access, no delete, gated by Cedar). Three consequences. **(1) Slack and the ✅ button are out of Mission Control entirely** — *"the reality of changes for the entire infra are too big to approve via a slack button, even a misclick"*. That also dissolves the audit's sharpest blocker: GitHub refuses to let any identity approve its own PR, so "the bot merges on ✅" was never implementable, and every workaround either invents a second bot (making author≠approver fiction) or removes branch protection. **The approver is a human with their own GitHub account, and that is the feature.** **(2) Mission Control is now PR-only by construction** — no MCP tools, no SaaS, therefore genuinely no Sentinel, which is what makes the "Sentinel has no role" sentence honest at last; capability profiles move to Airlock where real MCP powers live. **(3) The agent runs OUTSIDE the cluster and NOT on Sentinel's host** — owner's point: *"changing its OWN infra… that one should be robust so i can always have connectivity even if the entire stack is offline"*. An agent inside the cluster it repairs dies with it, taking the repair mechanism along, and `git revert` only helps if something is alive to propose the revert. It also cannot share Sentinel's host, because a process there can reach the loopback-bound admin API and hand itself its own approval surface. **Owner-found security fix, now closed:** `launch.sh` was `exec claude` with no MCP isolation, so the operator inherited the owner's personal MCP servers (Gmail, Drive, Notion) — the advertised "read-only cluster, PR-only GitHub" isolation was a half-truth. Now `--strict-mcp-config` with a deliberately empty config; anything the operator SHOULD have gets added explicitly and reviewably. **Renumbered while it was free:** Phase 6 = Mission Control complete (small), **Phase 7 = Airlock** (five sessions: ADR-005 · profiles+multi-user Sentinel · the door+OAuth · GitHub MCP with XAA · Slack MCP), Phase 8 = observability, Phase 9 = cloud.

- 2026-07-28 — **Consistency audit against the two flows: two of my own sentences were false, and one structural blocker was unscheduled.** Owner asked for a scan before starting Phase 6; an independent read found (a) `CLAUDE.md` claimed a grant now covers *a set of tools for a window* — **it does not**, it is still one tool per call, which is exactly what 5.5.8 measured as unusable, so a reader would plan on a primitive that does not exist; (b) *"Sentinel has no role"* in Mission Control was too absolute and contradicted the phase implementing it — Sentinel does not gate the PR path but absolutely gates the agent's non-PR actions. Both corrected, plus the revertibility claim now states its boundary (`git revert` restores declarations, not data). **Security fix:** the operator charter authorized "values-level changes", and `networkPolicy.enabled` is a value — un-fronting an MCP server was a one-line routine-looking PR reviewed by an owner who reads diffs but does not write code; security-relevant values are now explicitly out of bounds. **`catalog/echo` declared `exposes-mcp:"false"` while being the one workload the proxy fronts**, which would have made it invisible to the admission policy meant to protect it. **Five blockers recorded for ADR-005**, sharpest first: no identity can approve the agent's PRs (4.5's own negative test proved 422 then 405, and Phase 6 removes the bypass); Cedar has no *resource* so `DROP TABLE` on prod and staging are the same capability string; `confirm` has no home because self-elevation needs IdP identity while ADR-004 settles "Sentinel behind Authentik: no"; Authentik group membership is a second self-granting path; tenant scoping is due before flows exist, not in 6.5. Structure: CLAUDE.md build plan now carries **Phase 6.5 — Airlock** with six deliverables, Phase 6 renamed "Mission Control complete", and a Loki sequencing bug fixed in both docs.

- 2026-07-28 — **The two flows have names, and the approval model was wrong until today.** Owner pushed back hard on the friction — *"i'm trying to imagine 500 engineers calling an mcp server to delete things… if someone needed to delete something we would go to IT manually"* — and was right. Per-action approval does not scale; it is the six-approval finding at organisational scale. **Named and written into CLAUDE.md:** **Mission Control** (the platform operating itself: Claude proposes, GitHub reviews the diff, ArgoCD applies, `git revert` is the undo — Sentinel has NO role, because a second gate in front of a working gate is pure friction) and **Airlock** (the workforce reaching MCP servers: birthright entitlements by group cost zero approvals; dangerous power is *borrowed* for a window, never held). **The dividing line is revertibility** — if you can `git revert` it, review the diff; if you cannot, gate the elevation, not the action. **Four Cedar outcomes**, and `confirm` is what makes it scale: permit (just works, logged) / **confirm** (sudo-shaped — the caller confirms they meant it, time-boxed, recorded, no second person) / approve (a different human, rare by design — if it is not rare the policy is wrong) / forbid (`DROP TABLE` on prod: no button, ever, while the same tool on staging simply works because the tier differs). Elevation **replaces the IT ticket** rather than adding friction to a flow that had none. Sentinel's per-flow scope-locked expiring token is the elevation primitive for both flows — what changes is that a grant covers a SET of tools for a WINDOW instead of one tool for one call. Phase 6 order set: ADR-005 → agent elevation profiles → first real MCP server → control-plane Claude (Mission Control complete) → Airlock's public door. Also shipped `scripts/sentinel-demo.sh` so the console has something to show before any agent exists.

- 2026-07-28 — **PHASE 5.5 CLOSED — the load-bearing piece exists.** Owner ran `sudo scripts/sentinel-smoke.sh`; **all assertions passed**. Every exit criterion met: two systemd units outside the cluster (separate, so a console crash cannot stop enforcement), a real passkey held by the owner, a real MCP server that refuses unauthenticated traffic and permits granted traffic, a kill switch that kills mid-session and a release that does not resurrect, and an audit log carrying eight event types. **No MCP call now reaches a server without a human-approved, scope-locked, time-limited capability** — the gate ADR-001 made non-negotiable before any non-PR external power. Two bugs surfaced on the owner's first run and were fixed: `sudo` resets `HOME` so kubectl lost the kubeconfig (and reported an error that named nothing), and the battery called the console over plain http with no CA — it now reads the origin from `/etc/sentinel/sentinel.env` and follows whichever scheme the install serves. Built beyond the checklist because the work demanded it: mTLS on Sentinel's own CA, server-derived tool scope, claim-once tokens bound to a caller nonce, three CSRF layers under the passkey, both-direction NetworkPolicy, and the three-layer claim tested rather than asserted. Platform: 12/12 apps Synced/Healthy, four doors answering, both units active. **Next: Phase 6, resequenced by the six-approval finding — policy (Cedar/XAA ADR) BEFORE real MCP powers.**

- 2026-07-27 — **5.5.8 — the enforcement chain holds, and the GRANULARITY does not. Six approvals for one tool call.** Built `catalog/mock-mcp` (app #12): a **real** Streamable HTTP MCP server — stdlib-only in a ConfigMap on stock `python:3.12-slim`, so no image to build and nothing pip-installed at pod start — with the **official MCP SDK client as the arbiter of realism**. It completed a full session: initialize, tools/list, tools/call. **The headline, measured not estimated:** deriving the capability scope for every request one honest session makes gives **six distinct capabilities** — `rpc.initialize`, `rpc.notifications.initialized`, `rpc.transport.get` (the SSE channel), `rpc.tools.list`, `say`, `rpc.transport.delete`. One-grant-per-call does not survive contact with real MCP: six taps to say hello means the human abandons the console or rubber-stamps, and a rubber-stamped gate is worse than none because it manufactures the appearance of oversight. **Nothing is broken** — every layer worked exactly as designed — the granularity is wrong, which reclassifies the roadmap's *capability profiles* and *trust gradients* from polish to **Phase 6 prerequisites** (both policy problems → Cedar). Also proven: the **bodiless GET and DELETE work end to end** (the two verbs that had never met a real client and would have made MCP structurally impossible), and **the three layers refuse independently** — `delete_everything` is refused by the MCP server's own allowlist even when Sentinel granted the capability, a CLAUDE.md claim nothing had ever checked. mock-mcp's NetworkPolicy restricts **both** directions (egress DNS-only), closing ADR-004 debt 6 on the chart Phase 6 will copy. `scripts/sentinel-smoke.sh` joins the scripted battery, self-authenticating with a throwaway passkey it deletes (not a backdoor: minting an enrolment code already needs host privilege). One privileged run left to tick the gated assertions.

- 2026-07-27 — **5.5.7 DONE + the owner is enrolled — Sentinel is real.** Installed via `sudo scripts/install-systemd.sh`; both units active; **owner registered a passkey held by Microsoft's password manager in Edge** (recorded in SETUP.md Part 2 — there is no reset link, so a second device is the whole recovery plan). Getting there took three wrong diagnoses of one error and the lessons are worth more than the fix: (1) **I guessed instead of searching.** "The operation failed for an unknown transient reason" is `UnknownError`, and the documented match is Mozilla bug 1804624 — CTAP2 fails when no PIN is set, triggered by requesting `userVerification: required`, which our options did and webauthn.io does not. Fixed to `preferred` (password managers verify anyway and the result is now recorded per credential), and `residentKey` raised to `required` because login sends an empty allowCredentials. **When the owner supplies an exact error string, look it up.** (2) **I told the owner to `git pull`** — CLAUDE.md says plainly they never type git commands, and the code was already on their machine. (3) **I shipped an https change to fix an unconfirmed theory**, charging a manual cert import for it. Owner pushed back on both the diagnosis and the friction; **https is now back on the merits** (it is the production transport and "loopback today" is not a reason to ship a different one locally) with the CA trusted for the Windows user automatically by the installer — Edge/Chrome need no import, Firefox keeps its own store and still does. Setup is now **one command and one link**: the installer mints the enrollment code itself and prints `https://localhost:8400/#enroll=<code>` — the fragment never reaches the server, so the code stays out of access logs and nothing is copied between windows. The console also now reports the DOMException **name**, since the prose browsers attach is useless. Known quirk, not chased: Firefox in an RDP session still failed; owner will retest with Chrome as part of final testing after Phase 8.

- 2026-07-27 — **5.5.7 BUILT — and the scope changed for the better: build the CLOUD artifact, not a WSL one.** Owner directive when choosing systemd over a container: *"Windows is NOT its end state so we should ALWAYS be doing things for production in the cloud, otherwise we will be re-creating processes in phase 8 not just building out a small terraform change."* The reframe that matters: **systemd was never a local-only choice** — a droplet runs the same units — what would have made Phase 8 a rewrite is baking lab-isms in. Encoded so it can't be skipped: a new **CLAUDE.md working principle** ("build the cloud artifact first; the lab is one place it runs") plus **ADR-004's construction rule** with a wrong/right compliance table and the test *"would Phase 8 point Terraform at this, or re-create it?"*. Built accordingly: **two units** (`sentinel/deploy/*.service`) because a console crash must not take enforcement down (broker = hot path, fails closed); **deliberate deviation from the phase doc — `User=sentinel`, not `User=bob`** (a dedicated system account is both least-privilege and the cloud shape); `/opt/sentinel` for code, `/var/lib/sentinel` via systemd `StateDirectory`, `/etc/sentinel/sentinel.env` root-owned and NOT a `ConfigurationDirectory` so the trust anchor can't rewrite its own config; full daemon hardening (`ProtectSystem=strict`, `NoNewPrivileges`, `SystemCallFilter=@system-service`, …). **One install path** — `scripts/install-systemd.sh`, idempotent, the same script cloud-init calls; the ONE host-specific fact (broker bind address) is detected there and written to the env file, so the units know nothing about k3d; certs are adopted not re-minted so the cluster's trust survives. **Refuse-to-start guard** added: the console will not boot on a non-loopback bind over plain http — a `0.0.0.0` typo would otherwise put the kill switch on the network in cleartext while everything looked healthy — written as a **lifespan** handler, not the deprecated `@app.on_event`, because a security check that stops running when a framework drops a hook is a control with a silent expiry date. `enroll-operator.sh` is now install-aware (systemd install or dev checkout, one command). Units pass `systemd-analyze verify`; suite 13/13. **Reboot policy decided (owner): manual start, documented** in SETUP.md §1.7 — Windows doesn't start WSL2, so nothing else starts; `Restart=always` is true within a running WSL and is not a promise of unattended availability, and saying so plainly beats implying otherwise. **Install is an owner action** (needs root) — see Next action.
- 2026-07-27 — **5.5.6 DONE — the kill switch now needs a passkey, and there is no password to phish.** `webauthn` 3.0.0 + `pyotp` + `qrcode`(SVG — PNG would drag Pillow into the trust anchor); five tables (operators/credentials/sessions/enrollment_codes/challenges), migration `cdf028306ccc` with the audit-enum CHECK hand-widened **for the third time** (Alembic still doesn't diff CHECKs — now a reflex). **The design call that mattered:** registration is NOT "first run wins" — it needs a single-use code minted on the host by `scripts/enroll-operator.sh`, so becoming the approver requires already having the host, not merely reaching the port; the same command adds a second device, and a second device IS the recovery story (no account-recovery backdoor, because a backdoor is a second front door to the kill switch). Everything that reads or changes platform state now 401s without a session — **reads included**, since the pending panel and audit log describe what the agent is doing; `/healthz` stays open and uninformative for supervisors. `current_operator()` returns the verified human, so `granted_by` is cryptographic (the 5.5.5 one-file-change prediction held exactly). Sessions are DB rows, not signed cookies, so revocation is real and they survive a restart (asserted). **`localhost`, not `127.0.0.1`** everywhere — WebAuthn's RP ID must be a domain — including the Authentik tile. Tests drive a **real software authenticator** (P-256, real signature) so the four unphishability checks each get exercised: wrong origin / replayed challenge / unissued challenge / unknown credential, all 401. 12/12 across four suites, each standalone-runnable; plus a live ceremony over HTTP and the full chain ask → grant (401 without session, 201 with) → claim → **200 through the proxy**. Harness lesson: the Secure session cookie is silently dropped by an http test client — fix the harness, not the security property. Next: 5.5.7 systemd.
- 2026-07-27 — **Sentinel gets a portal tile, and a hard "no" to Sentinel-as-SSO-app is recorded.** Owner asked whether Sentinel eventually becomes an Authentik application; the motivation (find all the platform's apps in one place once a domain lands) is right, the auth coupling is not. **Rejected with reasons in ADR-004:** Authentik's groups are declared in THIS repo and applied by ArgoCD, and the operator Claude's job is opening PRs against this repo — so an ordinary-looking SSO-housekeeping PR would yield an identity able to approve Claude's own capability requests, granting power obtained without ever touching Sentinel's admin surface; a dead Authentik already locks every SSO login, and the kill switch must work exactly when the cluster can't be trusted; cluster-admin implies Authentik-admin implies impersonating the operator (`sso-dance.sh` uses that feature deliberately). Framing to keep: every app keeps a local break-glass for "Authentik is down" — Sentinel is the app where the break-glass IS the front door. **Accepted instead:** a generic `linkApps` mechanism rendering an authentik application with **no provider** — a portal bookmark that neither authenticates nor authorizes its destination; URL is a value (loopback locally, VPN-reachable in cloud). **Bug found by verifying as a user instead of with the admin bypass:** first cut bound the tile to a `platform-operators` group nobody was in, and authentik filters the portal per user — the bookmark existed and was invisible to everyone including the person who asked for it (and `GET /core/applications/` hides it without `superuser_full_list`, which made it look like the blueprint had silently skipped the entry). Now bound to the built-in admin group, **`!Find`-referenced not declared** — a blueprint writing to that group could reset its superuser flag and lock the owner out of authentik. Verified in akadmin's own portal listing: argocd, grafana, openwebui, sentinel. Corollary recorded: who may APPROVE is a deliberately smaller set than who may SEE, and answering both from one directory hands over the kill switch.
- 2026-07-27 — **5.5.5 DONE — the console exists, and hunting bugs paid for itself twice.** The GUI is one screen at `http://127.0.0.1:8400/`: pending cards (agent/tool/reason/countdown + the flow's recent audit lines) with **Grant 5m / Grant 1h / Deny**, derived-active flows, kill switch with two-step confirm + release, audit tail ×50. No build step, no CDN, strict CSP, `textContent`-only rendering, and a loud **NO CONTACT** banner instead of stale state. **Because the console is a web page, the API hardened first:** three CSRF/rebinding layers (Host allowlist, Origin check, required `X-Sentinel-Console`) *below* 5.5.6 auth, and **operator identity moved server-side** (`app/actor.py`; `*_by` body fields gone, `extra="forbid"` so old callers fail loudly) — making 5.5.6 a one-file change. **Bug found by probing the live path:** the capability token was reaching the upstream MCP server, replayable for its TTL — fixed with an HTTPRoute `RequestHeaderModifier` (runs after ext_authz, so the broker still validates); the same probe *cleared* client-forged `x-sentinel-grant-id` (overwritten by the auth response). **Then an adversarial review found six more**, all fixed with regression tests: token pickup could be **stolen** (dedupe handed any caller another's `request_id` and the poll authenticated nothing → caller-minted `claim_nonce`, wrong nonce = same 404 as unknown id); **live tokens written to journald** (query param → header); **MCP could never have worked** (bodiless GET/DELETE of Streamable HTTP denied unconditionally → now grantable `rpc.transport.*`, this would have blocked 5.5.8+Phase 6); duplicate-JSON-key **authorize-one-run-another**; **kill switch couldn't be pressed twice** (sweep sat inside `if not engaged`); SQLite **WAL+busy_timeout** (console read could block the audit write that would have explained the resulting denial). Also: `claim` audit type, dead TTL knob revived + ceiling 1440→60, cert renewal on expiry, Swagger-UI off. Suite 7/7 + live battery incl. hijack 404 / no-nonce 422 / 200 through proxy / kill→revoked→release-stays-dead / zero tokens in logs. **ADR-004 (Proposed): where Sentinel runs in cloud** — a named trust domain, VPC droplet outside DOKS (in-cluster ends the model, laptop breaks availability); Phase 8 gains a sixth deliverable; ranks the deliberately-unfixed debts. Method lesson: ArgoCD `Synced/Healthy` means "matches what it last fetched", not HEAD — compare `.status.sync.revision`. Next: 5.5.6.
- 2026-07-27 — **5.5.4 DONE — the Sentinel proxy: enforcement is live, and the client cannot pick its own scope.** `catalog/sentinel-proxy` (app #11): own GatewayClass+EnvoyProxy fleet (only IT holds Sentinel's client cert), Gateway-wide SecurityPolicy `extAuth` → broker `/v1/ext-authz` over mTLS (Sentinel's OWN CA via `mint-certs.sh` — cluster can't mint a cert the broker trusts; broker requires client certs, proven 3 ways). Design upgrade owner-approved then bettered: no Lua — EG `bodyToExtAuth` ships the original body and the BROKER derives `<server-from-path>.<params.name>` (pure, pytest-covered `app/scope.py`); composite scope kills lying-body AND cross-server reuse (both 403'd live). `sentinel-broker.internal` named in coredns-custom (no hardcoded IPs); echo consents via ReferenceGrant + ingress-allowlist NetworkPolicy in its own chart; bypass = NAMED Hubble `Policy denied DROPPED`; Traefik door intact; audit trail complete (request/grant/use/denial). **Latent bug found by the fleet's first pod:** ai-gateway-helm regenerates webhook certs per render → every catalog commit stranded the caBundle → Fail-policy webhook blocked ALL EG pod creation (incl. KEDA scale-ups) since the rebuild's first commit; fixed declaratively (ApplicationSet `ignoreDifferences` + `RespectIgnoreDifferences=true`) + one controller restart. EG-defaults diff loop bit again (3 pin commits — group/kind/weight now explicit + commented). Hubble UI on. Tests 3/3; battery 7/7. Phase-6 note: bodiless GET (MCP SSE channel) denies closed today — decide at Phase 6. Next: 5.5.5 GUI.
- 2026-07-27 — **5.5.3 DONE — the broker API, and the trust boundary is now physical.** Owner paused to review the endpoint count (right call): the phase doc's 4 endpoints couldn't close the loop — no token-delivery path, no Deny backend, no kill-release. Final = **9 endpoints across TWO listeners**: `app/broker.py` (request/poll/check) binds the k3d gateway IP for pods; `app/main.py` (grant/deny/kill+release/audit/flows) binds 127.0.0.1 — granting has NO route on the cluster-facing app (verified: POST grant on :8401 → 404). `app/service.py` centralizes every transition, each auditing in the same commit. Production-trap decisions: HTTP-status-as-verdict on check (200/403 = native Envoy ext_authz contract for 5.5.4), claim-once token delivery via poll (granter never sees the secret; only the hash persists), dedupe on duplicate pending, lazy expiry, kill=revocation persisted in `kill_state` (survives restart). Migration `80951989de42` (enum widened to 7 types, hand-added CHECK both directions — Alembic doesn't diff CHECKs). Proof: `tests/test_broker_flow.py` **20/20** (happy path, 4 deny reasons, dedupe, deny→409, kill revokes+blocks+survives-restart, release-keeps-old-dead, 7 audit types) + live two-listener curl walkthrough. `/docs` (FastAPI) is the human doc, code-generated so it can't drift. Note: an incomplete `sentinel/smoke.sh` appeared unauthored — left untracked, flagged. Next: 5.5.4 Envoy ext_authz proxy + NetworkPolicy.
- 2026-07-27 — **5.5.2 DONE — Sentinel's data model, guardrails proven at the DB.** Migration `d405f45ef0a6` (autogen inspected, downgrade/re-upgrade proven): `flows` (client-supplied string PK), `capability_grants` (UUID hex ids, real FK to flows, **token_hash SHA-256 unique — tokens never stored**, expiry/revocation columns), `audit_events` (autoincrement, strict 5-value enum + CHECK, **deliberately no FKs** so the log also records unknown/garbage flows). Behavior smoke 6/6 — including `PRAGMA foreign_keys=ON` wired per-connection (SQLite defaults OFF: without it the FK was decorative). Conventions in models.py: naive-UTC everywhere; grant valid = not-revoked ∧ not-expired ∧ kill-off. Next: 5.5.3 broker API (listener-split decision first).
- 2026-07-27 — **5.5.1 DONE — Sentinel is born (skeleton + trust model proven at layer 3).** Stack: Python 3.12 + FastAPI + SQLAlchemy/Alembic on SQLite (boring on purpose; `py_webauthn` at 5.5.6). `sentinel/` scaffold: app package with `/healthz` (DB round-trip), Alembic wired to app metadata (`SENTINEL_DB` env, batch mode for SQLite ALTERs), pins frozen. Verified live: migration no-op clean, healthz 200 on 127.0.0.1, and the one-way-trust probe — pod reaches host Ollama 11434 (200, control) but Sentinel's loopback-bound 8400 is **URLError-unreachable from the cluster by construction**. Learnings docs consolidated same session: **ADR-003** (Cilium + kube-proxy retained, the corrected premise, catalog citizenship test), CLAUDE.md premise fix, catalog README dependency convention (Chart.lock committed, tgz not). Host gap: `python3.12-venv` apt package needed (SETUP prereq line at 5.5.7). Next: 5.5.2 data model.
- 2026-07-27 — **PHASE 5.5 ENTRY CRITERIA MET — the Cilium rebuild IS the DR proof.** Teardown (self-heal paused → graceful scale-down → clean pg shutdown; backups: pg_dumpall 215 tables + 829M tar, unused) → `bootstrap.sh` end-to-end: eBPF mounts, **Cilium 1.19.6** (kube-proxy kept = DOKS parity; hubble+relay), docker CPU ceilings + kubelet system-reserved (nodes advertise 2 vCPU, 8 ≈ physical), CoreDNS override, Portainer agent + auto network-reattach, age key + **lab CA restore from new `k3d/lab-ca.enc.yaml`** (fingerprint identical after — client trust survived; cert-manager adopted), cert-manager CRDs pre-ArgoCD (run-1 failure: argocd's B7 TLS door needs the Certificate kind — chicken-and-egg fixed in-script), ArgoCD, auto-minted operator kubeconfig (read/write/secrets probes verified). **Drill caught two pets:** EG v1.8.1 + AI-GW v1.0.0 control planes were hand-installed 2.5 leftovers → now `catalog/envoy-gateway` + `catalog/envoy-ai-gateway` (pinned integration values recovered from the v1.0.0 tag; catalog 8→10 apps). **10/10 Synced/Healthy**, gate 5/5, `sso-dance.sh` **7/7** (headless SSO on warm DB), `netpol-smoke.sh` **3/3** + Hubble per-flow `Policy denied DROPPED` evidence. Premise corrected: k3s's embedded kube-router controller already enforced basic v1 netpol under Flannel (pre-rebuild smoke passed 3/3!) — Cilium's case is verdict observability, L7/identity headroom, DOKS parity. New session rule: probes assert SETTLED state (endpoint/policy propagation is async — one-shot probes race it). Commits `f617367`..`d544b30` + docs. Next: 5.5.1 Sentinel skeleton (tripwire: confirm scope first).
- 2026-07-27 — **PHASE 5 CLOSED — B6+B7+C in one push: SSO on ALL THREE doors, one-click tiles, docs converged.** B6 Grafana: blueprint v3 (grant_types from birth), canonical-issuer URLs + native `tls_client_ca` (no initContainer needed), roles→Admin/Viewer via `role_attribute_path`; snag: the `.gitignore` `*-secret.yaml` tripwire silently ate the new Secret template from `git add <dir>` (renamed `grafana-oidc-env.yaml`; RULE: verify commit file counts) — and kube-prometheus-stack full syncs sit through the admission-hook Job (minutes = normal). B7 ArgoCD (stretch): door `argocd.lab.local` (server.insecure was pre-set for exactly this), native oidc.config + inline rootCA, RBAC csv admins→admin/users→readonly; design catch: authentik's built-in `profile` mapping emits ALL groups — omit it on providers with filtered `groups` claims. One-click tiles: `meta_launch_url` → each app's OIDC initiation route. Dances verified everything: akadmin admin×3 (argocd can-sync=yes), bob user/Viewer/readonly (can-sync=no). C: README/SETUP/cheatsheet converged ("it's one login now" + break-glass table + `sops set` tip + argocd manual-helm posture), /resume gate step 5 now checks four doors (200/302/302/200). Commits `8537212`..`f92aef0` + docs. Next: Phase 5.5 entry = Cilium rebuild.

- 2026-07-27 — **Phase 5 B4+B5: SSO LIVE end-to-end (owner browser-verified: akadmin→admin, bob→user).** B4: OIDC provider+application assembled headless from blueprints (CM via chart hook, `!Env` client secret from SOPS, `state: present` = git beats UI drift); B4's rolling-update race understood (old worker consumed discovery tasks; cold bootstrap immune). B5: one canonical issuer for browser+backend; local shims = coredns `lab.local` template zone (172.18.0.1) + combined-CA-bundle initContainer, both ADR-002-named, both dead in cloud; blueprint v2 adds groups, IdP-side bindings (app literally vanishes from non-members' portals — accidentally proven), prefix-filtered roles claim; PKCE S256; redirect URI source-verified against v0.9.6 (B4's guess was wrong — strict match would have failed). **The bug a real click found: API/blueprint-created providers get EMPTY `grant_types`** (UI silently defaults it) → every authorize bounced `invalid_request`, masked by OpenWebUI as "email or password incorrect"; fixed in-blueprint, then the FULL login dance run headless (flow-executor→authorize→callback→who-am-I) as the new session rule: verify the live flow, not the objects (memory). Model visibility policy: gateway allowlist in git, `BYPASS_MODEL_ACCESS_CONTROL=true`, verified as impersonated bob. Break-glass: local form stays (dan@itdan.com). Commits `19c4106`..`b7de2d5`.
- 2026-07-26 — **Phase 5 stage B: B1–B3 done (Authentik live, akadmin owner-verified).** B1: postgres gains least-privilege `extraUsers` — app roles on fresh init via `20-extra-users.sh`, passwords through Secret env (never ConfigMap); live instance mirrored manually + password rotated. B2: `catalog/authentik` 2026.5.6 pinned (zero redis in render — the ≥2025.10 Postgres-only bet held) = ArgoCD app #8 ~2 min after push; server+worker Healthy; 215 tables migrated into platform postgres; 10/100 connections idle; akadmin bootstrapped headless (AUTHENTIK_BOOTSTRAP_* from SOPS). B3: `authentik.lab.local` hosts line (snapshot-first elevated script, receipt verified) + owner browser login — dashboard live, embedded outpost healthy, the owner's own typo visible as a failed-login event (audit works). **Near-miss → rule:** `sops -e` flags don't bypass creation rules; a redirect truncated the tracked postgres secrets file → git restore, regenerate, `ALTER ROLE` rotate, then encrypt **in place** at the rule-matching path; verify `ENC[` + round-trip before `git add`. Doors tables gained Authentik everywhere. Memory: `sops-encrypt-in-place`. Next: B4 blueprints.
- 2026-07-26 — **Phase 5 STAGE A COMPLETE (TLS foundation, one session) + ADR-002 accepted.** A1 `catalog/cert-manager` (v1.21.0, app #7, CRDs+keep, labels live-verified). A2 lab CA (`CN=homelab lab.local CA` → 2036, ECDSA-P256, key pinned Never; cross-ns smoke leaf openssl-verified). A3 per-service TLS pattern in `_template` (chart-owned cert + 301-redirect middleware + two-door IngressRoute), adopted byte-identical by openwebui+echo; 8443 was already declarative since Phase 2 — zero cluster surgery; liveness gate moved to the https door. A4 CA trusted on Windows (owner-verified padlock login; SETUP.md gains export→fingerprint→import(+undo) for Win+macOS; browser-restart gotcha documented). A5 Grafana door (`https://grafana.lab.local:8443`, root_url set pre-SSO, umbrella-owns-the-door pattern; hosts line via snapshot-first elevated script with receipt — RunAs-blocks-interop lesson captured). **ADR-002 "domain in, platform out" accepted** (owner, second scale push): one front door two targets (script local / Terraform cloud), 4-knob env contract, exposure policy (doors = browsed+authenticated; Prometheus/Alertmanager deliberately doorless), Authentik blueprints REQUIRED, DEPLOY.md single-doc, merge-queue trigger at >1 proposer, Phase 8 drill→product path. README: "Where it runs" topology + doors table + logins-in-ten-seconds. New memory: `cloud-parity-product-bar`.
- 2026-07-26 — **Catalog-contract v2 landed (issue #5, same-day turnaround).** Contract honesty first (B): `catalog/README.md` now names `_helpers.tpl` the ONLY byte-identical file (it IS the label API) and declares deployment/service a starting point charts outgrow — *unnecessary* forks are the drift, legitimate divergence isn't. Then the knobs (A): `_template` deployment gains with-guarded `command`/`args`/`env`/`envFrom`, documented in skeleton values. Convergence: echo's deployment file-identical to template again; openwebui + postgres `service.yaml` backported (postgres via `service.portName`); openwebui deployment's port name + probe refs parameterized. Proof discipline: 4 rendered outputs diffed byte-identical before/after, helpers asserted ×3, 4 charts lint clean, zero pod rollouts. Phase-05 doc unblocked; its exit-criteria-vs-slimmed-goal mismatch flagged for kickoff.
- 2026-07-26 — **Phase 4.5 CLOSED — the demoable product exists.** Loop 2 (rollback): PR #2 merged, **~29 s merge→cluster** (lesson: a *floor* drop rescales instantly when metrics are quiet — HPA stabilization guards *traffic* drops). Pre-merge, at the owner's idea, the **approval gate was negative-tested 3/3**: charter refusal ("merging my own PR is the one thing this operator role structurally can't do") → App self-approve **422** (platform, unconditional) → review-less merge **405** (`protect-main` — the single configurable control; `contents=write` cleared the token-permission layer). Operator filed evidence issue **#3** (first use of its charter's escalation path; it also self-corrected its earlier `admin:false` readout), builder answered with the admin-verified bypass list (repo-admin only; App absent). Gate test captured for adopters: SETUP.md "First flight: prove the gate" + README "The gate, tested". Loop 3 (onboard): **PR #4** births `catalog/echo/` from `_template` (operator pulled image config from the registry, test-ran the container under the chart's exact hardening, diffed every template against the skeleton, flagged its one deviation) → merged → **ArgoCD app #6** discovered, tier-label query + HTTP 200 verified. That deviation became issue **#5**: a catalog-drift survey proving every real chart forks deployment.yaml and the Phase-3 portName backport never propagated → accepted as **catalog-contract v2**, first item next session. **Demo GIF**: owner-recorded (ScreenToGif, self-edited 7000→364 frames), builder-shrunk 82→**9.1 MB** (no-dither palette + fast-band frame thinning + lossy) — leads the README. Multi-writer main all day: builder rebased over two bot merges. Phase exit criteria all met.
- 2026-07-26 — **Phase 4.5 day one: first bot-authored, human-gated change applied to the platform.** Repo flipped **PUBLIC** (GitHub Free won't enforce rules on private personal repos; full-history secret sweep ran clean first; AGPL/README/CONTRIBUTING already in place so it published as a launch, not a leak). Identity: **GitHub App** `itdan-homelab-operator` over PAT/machine-account (bot authorship, author≠approver enforcement, 1h tokens, uninstall kill switch). Ruleset `protect-main` (PR + 1 approval; admin bypass visible in push logs, removal = Phase-6 hardening). Operator: `view`-bound SA (refusals captured), own clone, `launch.sh`, charter at `ops/operator/CLAUDE.md`. **PR #1** "keep a warm spare (KEDA minimum 1→2)" — exemplary plain-English body, one-line diff — merged by owner, ArgoCD applied, 2/2 warm. Builder hit its first non-fast-forward against the bot's merge: multi-writer main era begins (pull --rebase norm). README gallery landed (scale arc + ArgoCD board). Paused before loop 2; cluster intentionally at 2/2 overnight.
- 2026-07-25 — **Phase 4 CLOSED** (third phase today). GitOps live: ArgoCD 10.2.1 (`catalog/argocd` umbrella, annotation-based tracking protects immutable selectors). Discovery = ApplicationSet over `catalog/*/argo.yaml` — file presence IS the deploy switch, `secrets:` flag explicit, `_template` excluded by construction. Repo access = repo-scoped **read-only deploy key** (write access to git does not exist in-cluster); SOPS bridge = pinned helm-secrets/sops via initContainer + age key as out-of-git bootstrap Secret. **All five catalog apps adopted Synced/Healthy** (keda first, postgres last with corrected `kubectl diff -n` preview — its 2.5.4 values drift reconciled, data intact). Auto-sync demo: commit→cluster in **50s**. Prune ON (offboarding = deleting a file). AIGatewayRoute controller-defaults diff loop fixed by rendering defaults explicitly. **bootstrap.sh + README.md landed** — bootstrap builds cluster+ArgoCD only; the platform self-assembles from the catalog. ArgoCD self-management deferred.
- 2026-07-25 — **Phase 3 CLOSED** (same day as 2.5 — two phases in one day). Monitoring: kube-prometheus-stack 87.19.1 as `catalog/monitoring` umbrella (25/25 targets — k3s component scrapes disabled = zero permanent noise; `.helmignore` `*.tgz` bug found+fixed template-wide: it breaks dependency loading for umbrella charts). Gateway metrics: PodMonitor in `catalog/ai-gateway` (port 1064 is pod-only), signal validated at ~24 tok/s. Autoscaling: CPU-HPA baseline re-targeted to the Envoy data plane (OpenWebUI is single-replica by design), demoed via EG `envoyHpa`, then superseded by KEDA 2.20.1 (`catalog/keda`) ScaledObject on `sum(rate(gen_ai_client_token_usage_sum{output}[1m]))`, threshold 30/replica, ceiling 3. k6 burst (in-cluster Job, own SOPS'd consumer key): 29/29 OK avg 34s, **1→3 in ~85s, 3→1 at +5min** (HPA stabilization ≠ KEDA cooldownPeriod — noted). `_template` service.portName parameterized. Incident: SOPS hand-append broke the consumers file → git-restore + python-validated rebuild; rule: build-validate-encrypt, never hand-append secrets.
- 2026-07-25 — **Hosts-file incident + recovery.** The stale-line removal script emptied `C:\Windows\System32\drivers\etc\hosts` (elevated `Get-Content` returned nothing; `Set-Content` wrote the empty result — no same-script backup, Claude's error). Restored known-good content (standard header + `openwebui.lab.local`), flushed DNS, verified resolution + HTTP 200 from Windows. Durable lesson → memory `elevated-writes-snapshot-first`: system-file rewrites snapshot first, in the same script. (Also: owner opted to skip the Sophos FP report; HMP re-enable reminder stands.)
- 2026-07-25 — **Phase 2.5 CLOSED** (timebox beaten — AI Gateway went v1.0.0 GA since ADR-001, erasing the pre-1.0 risk). `catalog/ai-gateway/` ships EG v1.8.1 + AI GW v1.0.0 as 10 labeled CRs: Gateway/GatewayClass/EnvoyProxy (ClusterIP, explicit resources), ClientTrafficPolicy (50Mi buffer — Envoy's 32KiB default truncates LLM bodies), AIGatewayRoute + AIServiceBackend + Backend (models data-driven from values), SecurityPolicy `apiKeyAuth` (per-consumer keys; auth matrix 200/401/401), and a stable Service via **computed ExternalName** (lesson: Services can't select pods across namespaces; EG's generated svc name ends in `sha256("<ns>/<gw>")[:8]`, so the chart derives it). OpenWebUI cut over (`gateway.apiKey` rename, base URL `http://ai-gateway/v1`) — owner browser-verified. Metrics confirmed at extproc `:1064/metrics` (`gen_ai_client_token_usage_*` et al). LiteLLM decommissioned: release + chart deleted, `litellm` + unused `openwebui` DBs dropped (postgres stays for Phase 5), references swept — `/resume` gate step 4 now tests the production path with the real consumer key. qwen3.5 gotcha: constraint-style canaries think-loop 2000+ tokens (`/no_think` ignored) — health checks use plain short questions.
- 2026-07-25 — **Ollama 0.32.4 flagged by HitmanPro.Alert as `CXrep/MalGo-A` at launch — triaged as false positive.** Evidence: flagged hash matches the on-disk exe byte-for-byte, Authenticode **Valid** signed `Ollama Inc.` (DigiCert EV), Windows Defender second opinion clean, detection name = cloud-reputation Go-binary heuristic on an hours-old release. HMP's Exploit-Mitigation exclusions did NOT stop the malware-engine block; owner disabled HMP malware protection entirely to relaunch Ollama. FP report drafted in Gmail (support@hitmanpro.com). Re-enable + re-verify backlogged.
- 2026-07-25 — **Liveness gate: first real run, 5/5 functional PASS** (post Ollama force-quit + update to 0.32.3). Full chain proven: pod → CoreDNS override → `172.19.80.1` → Ollama → qwen3.5 decoding. Throughput was ~0.05 tok/s: a Steam UE title (Windrose) + the 5.6 GB model exceeded the 4070's 12 GB VRAM → NVIDIA driver sysmem fallback. New norm: no GPU-heavy games during load tests/demos; gate doc now says to check `nvidia-smi` when generation crawls. **Adopter-docs contract** (owner input: "stupid easy for humans to maintain/update once open-source") added as a CLAUDE.md working principle + scheduled backlog items.
- 2026-07-25 — **ADR-001 re-plan accepted** (owner restated the goal: one Claude plane running/scaling the platform, then open-source it). Decisions: LiteLLM out (its OOM/prisma-root/rolling-tag record + owner verdict) → **Envoy AI Gateway** in new Phase 2.5 with Bifrost fallback; Prometheus moved from Phase 7 → Phase 3 (KEDA needs it — sequencing bug); **Phase 4.5 Control-Plane v0** added (PR-only operator Claude; Sentinel rule narrowed to "before any NON-PR power"); Phase 5 slimmed; Sentinel proxy will be Envoy `ext_authz`; open-source track (LICENSE/README/ADRs/demo GIFs) runs in parallel. CLAUDE.md build plan + phase docs rewritten to match; new docs: `docs/adr/ADR-001-*`, `phase-02-5-*`, `phase-04-5-*`.
- 2026-07-25 — **Track 0 repair.** Session-start liveness check caught a **silent chat outage**: k3s had rewritten CoreDNS `NodeHosts` (~June-28 restart) and dropped the k3d-injected `host.docker.internal` entry; lookups fell through to Windows DNS → stale Docker Desktop hosts line (`192.168.50.102`, unreachable) → LiteLLM→Ollama dead while all pods showed Running. Fixed declaratively: `k3d/coredns-custom.yaml` (a configmap k3s never touches) + CoreDNS restart; in-pod resolution verified → `172.19.80.1`. Full e2e re-verify deferred: Ollama was mid-update on Windows (OllamaSetup running, port dark) at session end. Also: `.wslconfig` finally created (32 GB / 8 proc, NAT kept; applies next restart); `/resume` gained Step-0 liveness gate; phase-01/02 doc hygiene fixed; owner stopped the Mullvad containers (out of scope; leak-verify before next use).
- 2026-06-03 — **Phase 2 CLOSED.** OpenWebUI chat verified end-to-end in the browser (owner confirmed: signup + `qwen3.5` reply worked) from the Windows box. Operator cheatsheet gained a chat-stack access-URL table + the `*.lab.local` hosts-entry guide. All Phase 2 work (P2.7–P2.11) committed and pushed to `itdan-com/homelab` on `main`. Next: Phase 3 (HPA + KEDA autoscaling).
- 2026-06-03 — P2.9/P2.10 + virtual-key path landed (chat stack live in-cluster). Owner chose virtual keys over master key (least-privilege). Wired LiteLLM→Postgres (dedicated `litellm` DB; postgres chart now auto-creates `auth.extraDatabases` via an initdb ConfigMap on fresh rebuilds). Deep snag: the LiteLLM image bakes the prisma query-engine into root-only `/root/.cache`, so the chart's non-root securityContext broke DB mode (`Not connected to the query engine`); a debug pod proved root mode works. Fix: run litellm + openwebui as **hardened-root** (uid 0 but drop ALL caps + no-priv-esc + seccomp RuntimeDefault) — the load-bearing least-privilege controls for a gateway are the scoped key + NetworkPolicy + SSO, not the container UID. Minted a `qwen3.5`-scoped virtual key → SOPS-encrypted into `catalog/openwebui/secrets.enc.yaml`. openwebui chart (pinned `v0.9.6`, `strategy: Recreate`, hostPath PVC) authored via subagent + independently verified. All three charts `deployed`; in-cluster e2e returns `PONG` from OpenWebUI's own pod through the virtual key. Zero downtime across 7 failed litellm upgrades — RollingUpdate surge kept the old pod serving the whole time.
- 2026-06-02 — P2.8 closed out. `catalog/litellm/` shipped: ConfigMap (model list rendered from values), Secret (SOPS-encrypted master key + slots for OpenAI/Anthropic/Gemini keys), Deployment (with config+secret checksum annotations for rollout-on-change), Service, IngressRoute. Six catalog labels propagate; `llm-traffic: "true"` confirmed. SOPS-encrypted master key (`sk-...`); git grep verified no plaintext leak. Hit OOMKill on first install — bumped to requests: 512Mi / limits: 2Gi (LiteLLM's Python connectors eat ~1Gi at startup even with one model); documented inline with the floor reasoning. **End-to-end test passed:** chat-completion request from a curl pod returned "PONG" from qwen3.5 via Ollama on the host. Whole LLM pipe is live.
- 2026-06-02 — P2.7 closed out. Cluster YAML now mounts `/home/bob/homelab-data` into every node at `/homelab-data` (the load-bearing change for persistent storage). Approach pivoted mid-task from Bitnami subchart to hand-rolled — Bitnami's `commonLabels` workaround would've been a wart on the catalog contract; hand-rolling fits natively via `_helpers.tpl` and gives more learning value. `catalog/postgres/` shipped: Chart.yaml + values.yaml + Service + Secret + PV + PVC + StatefulSet, all six labels propagating. Lint clean. SOPS-encrypted password (first real encrypted secret in repo). **Persistence proof:** wrote two rows → killed the whole cluster → recreated → reinstalled chart → rows still there with same IDs and timestamps. Documented gotcha: hostPath PVs don't honor fsGroup in k3s; one-time `sudo chown -R 999:999 ~/homelab-data/postgres-<release>` required per release. Also closed STATUS backlog item: canonical Portainer Agent manifest now declarative at `k3d/portainer-agent.yaml`. CNPG migration to operator pattern planned for Phase 7.
- 2026-06-02 — P2.6 closed out. `catalog/README.md` (231 lines) lands the six-label schema (four chart-level annotations + two release-level values for `tier`/`data-class`, enabling sandbox→dev→prod GitOps promotion) and the SOPS-secrets-with-Phase-6-migration-plan. `catalog/_template/` scaffolded with `_helpers.tpl` carrying the label-propagation contract; smoke-tested live (pod went 1/1 Running, IngressRoute live under `traefik.io/v1alpha1`, label selectors return the right deployments). SOPS+age installed (age 1.1.1, sops 3.13.1, helm-secrets 4.6.5); encrypt/decrypt round-trip verified via both `sops` and `helm secrets`. `.sops.yaml` at repo root with `encrypted_regex` so only secret VALUES are encrypted (key names stay diff-readable). `.gitignore` hardened with SOPS plaintext safety net. Trust-gradient design saved to memory.
- 2026-05-17 — Phase 2 groundwork session: P2.1 verified Ollama end-to-end (Windows binding fixed via `setx OLLAMA_HOST`, generation through pod->host.docker.internal->qwen3.5:9b returns in <5s). P2.2 `git init` + first commit. P2.3 declarative cluster config at `k3d/devlab-cluster.yaml`. P2.4 cluster rebuilt — scheduler-visible RAM dropped from 124 GiB to 28.7 GiB (the win). P2.5 Portainer auto-reattached. P2.12 pushed to private GitHub: itdan-com/homelab. Two new operational gotchas captured in commits: k3d memory suffix uses `g` not `Gi`; CoreDNS needs a post-create pod-delete to honor hostAliases (race against upstream cache).
- 2026-05-17 — Phase 1 closed out. systemd verified pre-enabled; docker-ce 29.5.0 + kubectl 1.32 + helm 3.21 + k3d 5.8.3 installed; Portainer CE 2.39 deployed and wired to the 4-node devlab cluster via the Portainer Agent (CE doesn't have kubeconfig import). Three architecture gaps surfaced and parked in backlog: Flannel→Cilium CNI swap before Phase 5.5, k3d node resource over-reporting, missing `.wslconfig`. Operator cheatsheet written at `docs/operator-cheatsheet.md` covering health checks, log inspection, Slack-diff decision protocol, and recovery.
- 2026-05-16 — Build scaffolding bootstrapped: STATUS.md + 8 phase docs created. "How sessions work" section added to CLAUDE.md.
- 2026-05-16 — Phase 5.5 (Sentinel) inserted into architecture as the load-bearing security broker. Kill switch moved out of the cluster to a systemd unit on the WSL2 host (trust-domain separation).
- 2026-05-16 — Plan expanded to 8 phases (was 6). Service catalog pattern + MCP cross-stack control plane + ChatOps+GitOps approval flow locked in.
- 2026-05-16 — Switched from Docker Desktop to docker-ce; Portainer added as the Docker GUI layer. Previous k3d state lost; needs re-creation.

---

## Owner decisions waiting (raised once — they live here, not in every session)

_Nothing is blocked on these. A future session should NOT re-raise them
unprompted; the owner picks them up when they want to._

1. **ADR-004 status** — still `Proposed`. It has since grown the
   cloud-artifact construction rule, a provider-neutral cloud shape with
   an AWS/GCP/Azure/DO table, the "don't expose the console, tunnel to
   it" recommendation, and three portability gaps. Say Accepted (or
   amend) and it becomes binding rather than suggested.
2. **The WAL commit** (`3c261b1`) — two expired, scope-locked capability
   tokens sit in public git history. Leave it (they are dead and useless
   without a private client cert), or rewrite history and force-push.
3. **Which cloud Phase 9 targets** — that is the one Terraform module
   written per provider; everything above it is identical. **Owner
   signal 2026-08-22:** *"the end state is getting this into cloud
   via a terraform script that just pushes this to like aws k8s i
   think"* — AWS (EKS-shaped) is now the lean, displacing the
   original DigitalOcean default. Not closed: "i think" is a lean,
   and `docs/cloud-cost-shape.md`'s finding (k3s-on-VMs ≈ half the
   cost of the default AWS shape, whose bill is mostly NAT/ALB/IPv4
   idle tax; nothing here needs managed k8s) gets re-presented when
   Phase 9 starts so the choice is made with the numbers in view.
4. **Demo assets** — `scripts/sentinel-demo.sh` now makes the console
   show a real pending request; Hubble UI is `kubectl port-forward -n
   kube-system svc/hubble-ui 12000:80` then `http://localhost:12000`,
   with `scripts/netpol-smoke.sh` generating the denied flows to look at.
5. **Mint the operator's own Anthropic key** (Phase 6.3's one console
   action; raised 2026-08-01). SETUP.md §1.8: workspace + monthly spend
   limit + key + one line in `~/.config/homelab-operator/env` (edit with
   nano, not echo). Until then, tick agent passes bill your own Claude
   login and log `auth=login`; worst-case burn at the guards' ceiling is
   ~$15/day if the platform stays broken all day.
6. **Where does the operator live?** (Phase 6 open question 1, carried
   at close.) Today: the WSL2 host — Sentinel's host — which CLAUDE.md
   property 2 forbids. Mitigations exist (granting requires a WebAuthn
   session; Sentinel state is owned by `sentinel`). Options: accept as
   a NAMED interim deviation until Phase 9 gives it its own VM
   (recommended), move to the Mac (sleeps), or a dedicated device now.
   Deciding also means correcting CLAUDE.md's "today that is the
   owner's workstation" line at the next architecture edit.
7. **Grafana arc screenshot** for the demo record (dashboard "AI
   Gateway — Token-Rate Autoscaling", 2026-08-02 morning window — the
   square waves + replica staircase). Drop it next to
   `docs/demos/mission-control-three-declines.md` when captured.
~~8. **ADR-007 status**~~ ACCEPTED 2026-08-15 (owner: *"lets get
   startedd?"*). `docs/adr/ADR-007-airlock-hardening.md`. Build order
   is priority order — Decision 1 (velocity) built same day; see
   backlog for what it does and does not cover.
~~9. **ADR-008 status**~~ ACCEPTED 2026-08-22, same day as proposed
   (owner: *"accept 7.8 i guess? … gotta get started on some of this
   after we land"*). `docs/adr/ADR-008-bring-your-own-idp.md` is
   binding; Phase 7.8 (external identity: door genericization →
   broker mode → EMA receiver) queues after Phase 8's two remaining
   items unless the owner pulls it forward.
~~10. **ADR-009 status**~~ ACCEPTED 2026-08-22, same day as proposed
   (owner: *"accept and pushit"*).
   `docs/adr/ADR-009-github-outage-posture.md` is binding; the
   hardening session (tick surgery, mirror, probe pass + runbook,
   sync-Unknown alert rule) is green-lit, queued after ADR-006's
   build, before or interleaved with 7.8 at the owner's choice.
   (D5's sync-Unknown rule already landed with the ADR-006 build.)
11. **Prometheus's scrape identity reaches every broker route**
   (found by ADR-006's build review, 2026-08-22; named in ADR-006's
   as-built addendum §5b). The broker has no per-cert authorization —
   mTLS is the whole gate — so `sentinel-prometheus-client` in the
   `monitoring` namespace can file capability REQUESTS with
   attacker-authored console text and poll/claim them, not just
   scrape. It cannot GRANT (your passkey tap stays the only yes),
   reach the admin API, or touch the kill switch. Options: accept as
   named (current state), or green-light the narrowing — a dedicated
   metrics listener validating a metrics-only sub-CA (small design
   pass + one more unit flag).
~~13. **Phase 9 substrate: k3s-on-EC2 vs EKS**~~ RESOLVED 2026-08-24 (owner: *"i think your recommendation is fine"*) — k3s-on-EC2 is the substrate, EKS the documented second module. Original entry: (ADR-010, Proposed
   2026-08-24). The ADR RECOMMENDS k3s-on-EC2 (half the cost, exact
   lab parity, no lock-in; EKS's Auto Mode can't run our Cilium) with
   EKS as a documented second module. This is deliberately the
   owner's call — it runs counter to the stated "aws k8s" instinct,
   and if EKS is wanted for a real client or portfolio reason that's
   a legitimate override. Accepting ADR-010 picks the substrate and
   greenlights the (multi-session) Phase 9 build.
~~12. **An Okta developer org for 7.8.1's live matrix**~~ RESOLVED
   2026-08-23 — the owner HAS one: `https://integrator-4949708.okta.com`
   (Integrator Free Plan). Probed via public metadata same day: org AS
   + `default` custom AS both live, RS256 id_tokens, client_secret_basic
   supported — compatible with the door's every assumption. NOT yet
   XAA-enabled (no token-exchange in either AS's grant types — ID-JAG
   minting needs Okta's EA flag via developers@okta.com, or xaa.dev
   covers the receiver's live leg). The live matrix is fully staged:
   `sentinel/scripts/okta-register-door.sh` (registers the app for the
   owner via a one-time, revoke-after API token) then
   `okta-live-matrix.sh` (no-root dev stack against the org, headless
   half automated; the owner's one act is signing in — which IS the
   test).
11. **Prometheus's scrape identity reaches every broker route**
   (found by ADR-006's build review, 2026-08-22; named in ADR-006's
   as-built addendum §5b). The broker has no per-cert authorization —
   mTLS is the whole gate — so `sentinel-prometheus-client` in the
   `monitoring` namespace can file capability REQUESTS with
   attacker-authored console text and poll/claim them, not just
   scrape. It cannot GRANT (your passkey tap stays the only yes),
   reach the admin API, or touch the kill switch. Options: accept as
   named (current state), or green-light the narrowing — a dedicated
   metrics listener validating a metrics-only sub-CA (small design
   pass + one more unit flag).
12. **An Okta developer org for 7.8.1's live matrix** — ADR-008's
   exit criterion wants the door proven against a real external IdP,
   and Okta's free dev org is the named first target (it is also the
   XAA counterparty for 7.8.3). Creating the account is an identity
   signup only the owner can do; after that, `docs/idp-registration.md`
   §Okta + one probe session closes the criterion.

## Backlog (captured mid-work, not yet scheduled)

_Items noticed during a phase that don't belong to the current phase. Promote to a phase doc or a memory file when ready. Delete if no longer relevant._

- **NEW failure mode at the 2026-08-16 reboot: Docker's boot-order IP lottery reshuffled the k3d node-container IPs, and Cilium's node registry kept the old ones — cross-node datapath dead, cluster DNS blackholed, five pods crash-looping, three doors down.** Distinct from the 2026-07-25 NodeHosts incident (`coredns-custom` survived intact and was NOT the cause). Mechanics: docker assigns `172.18.0.x` in container start order, so a reboot can permute them (server-0 landed on `.3` this time); the Kubernetes Node objects re-registered with the new IPs immediately, but the `CiliumNode` objects' `.spec.addresses` kept the pre-reboot IPs (including a stale/real collision on `.3`), so inter-node tunnels aimed at wrong addresses — every cross-node packet including pod→CoreDNS DNS queries (`10.43.0.10:53 i/o timeout` in three different pods' logs) blackholed while every pod-local readiness probe stayed green. **Fix applied same session:** `kubectl -n kube-system rollout restart ds/cilium` (agents re-register current IPs), plus a second bounce of the server-0/agent-2 agents whose node-ICMP health endpoints hadn't re-announced; verified CiliumNode==docker reality, cilium-health 4/4, DNS, all four doors, the LLM path, and 15/15 ArgoCD apps. **Durable fix to decide when bootstrap or the gate is next touched:** (a) k3d cannot pin per-node static IPs in its config, so the candidate is a bootstrap/boot-time guard — compare `CiliumNode .spec.addresses` against Node `InternalIP` and restart the cilium DS on mismatch; (b) add that same one-line comparison to the `/resume` liveness gate so the next reshuffle is caught in seconds, not diagnosed from crash loops (this reboot cost ~25 min of convergence-then-diagnosis).

- **GitHub is a single point of failure in FOUR separable ways — owner question 2026-08-22 ("what do we do about mission control's single point of failure when github goes down?"), four-agent assessment, verified against the repo and ArgoCD maintainer statements. Scope as ADR-009 / one hardening session when the owner green-lights.** What survives: running workloads (ArgoCD is not in the data path; apps flip Unknown ~3-6 min in and selfHeal STOPS enforcing — maintainer-confirmed, argo-cd discussion #21072), and Sentinel entirely (policy store is a LOCAL git repo with no remote, policy.py:396-400; kill switch/audit/elevation unaffected; only Airlock's github MCP tools die, which is natural). The four breaks: **(1) The tick dies BEFORE its own envelope check** — launch.sh:39-43 mints a GitHub App token + fetches under `set -euo pipefail` at the top of every run, so GitHub-down aborts the tick before envelope-check.sh (whose header says "NO GitHub token") ever runs: the platform loses its 5-min watchman exactly when ArgoCD divergence starts, silently (no OnFailure=, heartbeat just stops, nothing alerts — and interactive mode is gated by the same block, so the owner can't even hand-drive the operator). Asymmetry in code: Anthropic-down = logged `agent-error` verdict; GitHub-down = unlogged abort. Every escalation path (PR, issue) is GitHub; there is no second channel of any kind. **(2) The ArgoCD repo-server initContainer wgets sops+helm-secrets from github.com release URLs on EVERY pod start** (catalog/argocd/values.yaml:197-206) — any repo-server restart mid-outage (e.g. the reboot IP-lottery above) sticks in Init and ArgoCD can render NOTHING; compounding: charts/ tarballs are deliberately gitignored, 4 of 8 charts' lockfiles resolve to GitHub Pages (argoproj/kedacore/grafana ×2/prometheus-community .github.io), at least six distinct ghcr.io image repos live in the cluster (three pinned in catalog values + authentik's and KEDA's subchart defaults — ADR-009 review corrected the original "3 images" undercount), no registry mirror — so a REBUILD during a GitHub outage is impossible (bootstrap.sh:157 `helm dependency build catalog/argocd` hits argoproj.github.io). **(3) The re-entry hazard**: during the outage break-glass kubectl edits stick (apps Unknown = nothing enforces), but the moment GitHub recovers, the first refresh marks OutOfSync and selfHeal SILENTLY REVERTS every uncommitted manual fix — any runbook must pause auto-sync first (kubectl patch the Application CR / AppProject deny sync-window with manualSync:true — both work offline) and commit manual changes BEFORE re-enabling; note the ApplicationSet re-stamps child syncPolicy at recovery, so lift the pause in the right order. **(4) No mirror exists anywhere** — one remote, operator's clone points at the same GitHub URL, in-cluster deploy key is deliberately read-only. Recommended, priority order: **P1** reorder the tick (envelope check before token mint; `github_unreachable` becomes a logged verdict like `agent-error`, not an abort; `-m` timeouts on gh-app-token.sh curls; add GitHub reachability as an 8th envelope check; interactive mode degrades read-only instead of refusing) — small script surgery, highest value. **P2** local bare mirror on the host (post-commit hook or timer, `git push --mirror` gotchas researched: it force-pushes, verify by cloning back) + stop fetching sops at pod start (vendor the binaries or bake an image — also a supply-chain smell today) → DR becomes provider-independent, ArgoCD gains a repoint target (repoURL edit works live; expect the #25455 stale-history "not our ref" wrinkle). **P3** a one-page break-glass runbook in the operator cheatsheet (pause-first, log loudly, commit-before-reenable, Google-SRE-style noisy glass) — needs a live probe pass first (block github.com via CoreDNS override on devlab; measure the ~3-min "fight window", confirm `automated.enabled=false` is honored by our ArgoCD version). **P4** the non-GitHub alert channel is the EXISTING Phase 8 Slack-webhook item + an `OnFailure=` hook on operator-tick.service — GitHub-down currently means zero push notification to a human. NOT recommended: a warm Gitea/Forgejo PR-review standby (pull mirrors are read-only re PRs — go-gitea#6054; conversion is one-way; for ~1h outages, review-over-the-mirror + post-hoc PR reconstruction is the honest gate) and agent proposal-queueing machinery (no industry precedent exists; nearly free later as local branches on the mirror). Reality check: git-operation outages are the RAREST GitHub failure (~19 incidents/yr vs 57 for Actions; typical ~1h, e.g. the 64-min 2025-11-18 TLS-cert outage), so "wait it out" stays the default for the change pipeline — the fixes protect the eyes (P1), the rebuild (P2), and the recovery moment (P3).
- **kubelet (3 of 4 nodes) and apiserver scrape targets have been DOWN for at least the full 3-day metrics retention window — found 2026-08-22 while diagnosing the Cilium recurrence, and it is NOT that incident's residue.** Pod→node-IP connections refused (kubelet :10250, apiserver :6443 at 172.18.0.x) while pod→pod scrapes are green post-fix; every affected panel quietly shows one node's data. Nobody noticed because no alert watches `up{job="kubelet"}`/`up{job="apiserver"}` — the same shape as the Phase 8 "rules that can never fire" lesson, one level down. Needs its own diagnosis pass (likely k3d/Cilium node-IP routing, possibly broken since the 2026-08-16 reboot or earlier); consider an `up`-based meta-rule so a silently-dead scrape JOB pages, since per-target rules can't fire on data that never arrives.
- **Loki's in-cluster surface is open (:3100, no auth, no NetworkPolicy) — named in ADR-006's as-built addendum §5c, backlogged as its own careful pass.** Any pod can push forged `source="sentinel"` lines into the audit COPY (the canonical record is untouched; forged lines lack valid chain hashes but nothing verifies the copy today) and any pod can QUERY the copy — a confidentiality expansion over the Sentinel-host original. Fix shape: NetworkPolicy/CiliumNetworkPolicy restricting :3100 ingress to Alloy, Traefik and Grafana — needs a real flow enumeration first (ruler? self-monitoring?) per probe-the-live-path; the eventual divergence check is chain-verifying the Loki copy against Sentinel's.
- ~~**Two pre-existing Sentinel door defects found by ADR-008's adversarial review (2026-08-22)**~~ **BOTH FIXED in 7.8.1 same day**: the `/link` infinite redirect loop (cookie now door-wide, regression-tested against the server's actual Set-Cookie) and writerless `disabled_at` (console sign-in switch, killing browser sessions too). Kept struck-through so nobody re-scopes them.
- **Per-tool visibility inside an assigned server (competitive research, 2026-08-15, deepened same day) — closes ADR-005's named "gap 2" accepted limitation, and now has TWO independently-shipped reference implementations.** Today, within a server your group is assigned, tool NAMES stay visible in `tools/list` even where the specific call would be denied — rewriting that response live was judged structurally out of reach for our ext_authz enforcement point when ADR-005 was written. **Okta**'s MCP server builds a per-session filtered tool registry at connection time, keyed off the token's already-known granted scopes — cheaper than intercepting a live response per call. **AWS Bedrock AgentCore Gateway does the same thing through Cedar itself** — `tools/list` is a "meta action" evaluated by the same Cedar policy engine we use, gated by a dedicated IAM permission (`bedrock-agentcore:PartiallyAuthorizeActions`) separate from the per-call check. AWS's version is the more directly portable reference precisely because it's the same policy language — worth reading its API shape before designing ours. (Salesforce sidesteps the problem entirely at build time — admins curate a fixed per-agent tool list in Agent Builder, no runtime rewriting needed at all — a different framing worth naming even though it doesn't fit Sentinel's dynamic per-caller model.) Kept OUT of ADR-007 deliberately — different shape of gap (visibility filtering, not a policy-decision axis) and ADR-007 is already sitting unaccepted; growing it further just delays it more. Scope as its own small ADR once someone works through the AWS/Okta mechanics in detail.
- **ADR-007 Decision 1 (velocity) has a live, open-source, Cedar-native reference implementation — read it before building, found 2026-08-15, one week after Decision 1 was proposed.** AWS shipped "temporal policies" for AgentCore on 2026-08-06, powered by a new Apache-2.0 policy language called **Dogwood** (`github.com/dogwood-policy/dogwood`, verified live) that EXTENDS Cedar itself with native operators for exactly this — `count_within`, `count_distinct_within`, `sum_within` (rolling-window counts as first-class conditions inside a Cedar `when` block, evaluated by the same engine that makes the permit/forbid call). This is not "a competitor also thought of this" — it's the literal `context.actions_in_window` mechanism ADR-007 Decision 1 proposes, already designed, open-sourced, and Cedar-native. The reference interpreter's own README says it is not intended for production use, so this is prior art to study — the grammar/semantics, not a drop-in dependency — before hand-rolling our own. **Independent third confirmation the underlying idea is right**: Microsoft Entra ID Protection for Agents runs a first-class "Agent risk" condition (sign-in-frequency spikes) directly inside Conditional Access policy authoring, not as a bolted-on dashboard — same architecture, third vendor, agrees with the PAM/UEBA research ADR-007 was already built on. Caveat worth keeping: Microsoft's own docs say those specific risk detections are async/offline, not synchronous-blocking, which matches the distinction already drawn in ADR-007 between simple inline threshold counting (what we're building) and heavier ML behavioral baselining (explicitly deferred).

~~- **ADR-007 Decision 1 — velocity as a Cedar context flag.**~~ BUILT 2026-08-15 — `context.actions_in_window` (`_1m`/`_1h`), computed by `service.actions_in_window()` from a new `AuditEvent.tier` column + composite index, threaded into every context `ladder.decide()` builds (baseline/elevated/approved, computed once so a forbid fires at every rung). 9 tests, order-independence proven both directions with test_ladder.py. Full detail and the adversarial-review findings below in ADR-007's own "Decision 1 — BUILT" section — two fixed inline (schema= wasn't enforcing `required` at request time, a real latent gap since ADR-005 D3, not just this decision's; the `_1m`/`_1h` windows had zero test coverage distinguishing them — both regression-guarded and mutation-tested), two documented as limitations (per-tool not per-server keying; case-sensitive tier matching inherited from Decision 3, now also a velocity-evasion vector), and **one real scope boundary that needs a read, not just a skim**: velocity only protects Airlock's person/Cedar-door path (`ladder.decide()`) — `check_capability()`, the function behind `/v1/ext-authz` that the classic pre-Airlock flow-scoped capability mechanism still uses, evaluates no Cedar and got no velocity check. Assessed as low current exposure (Mission Control is PR-only and touches no MCP; the Airlock door mints its own forwarding tokens rather than using that classic loop) but NOT decommissioned and NOT a one-line fix (no principal, no tier concept, no policy-authoring surface on that path at all) — see the next backlog item.
- **Velocity gap: `check_capability()` (the classic flow-scoped MCP capability path) has no rate/velocity protection at all — found by adversarial review 2026-08-15, deliberately not bolted on same-session.** Every in-cluster MCP call's actual per-request gate is `check_capability()` (behind `/v1/ext-authz`), not `ladder.decide()` — the newly-built velocity signal lives only in the latter. The classic Phase-5.5 `POST /v1/capability-requests → poll → grant` loop (still live, still documented in `sentinel/README.md`) redeems its token entirely through `check_capability()`, which does a kill check + token/scope/expiry match and nothing Cedar-shaped — no principal concept for an anonymous flow-scoped grant, no tier derivation, no policy-authoring surface of any kind on that path. Today this looks low-exposure in practice (Mission Control is PR-only by construction and calls no MCP tool per CLAUDE.md; the Airlock door bypasses the classic loop with its own per-message forwarding tokens, which DO flow through `ladder.decide()` first) — but the path is not decommissioned, and the day any future in-cluster automation requests a capability directly instead of going through the door, it inherits zero velocity protection, silently. **Needs an actual design pass before building**, not a quick patch: what identity does a flow-scoped grant (no `principal_id`) key a rate limit on — `flow_id` alone? — and what's the threshold-authoring story given that path has no Cedar policy to write a `forbid` into at all (a config-level ceiling? a second, simpler mechanism?). Priority: high — this is arguably closer to the "runaway agent, unattended, 3am" scenario than the person-door path is, precisely because it's the one path an unattended agent (not a signed-in human) could actually use.
- **Velocity authoring trap: a per-tool key means a rule that reads "cap this server's writes at N/hour" is actually N × (number of distinct write tool names) — found by adversarial review 2026-08-15.** `service.actions_in_window()` keys strictly on the literal tool string, but every other primitive in this system (matrix forbids, permits) conditions on server/tier/action, which is what a policy author will pattern-match when hand-writing a velocity rule in the overlay. `policy-example`'s own github write set has 5 distinct tool names — a rule gated on `context.actions_in_window._1h >= 2` for github writes allows up to 10 actual writes/hour if the caller rotates across tool names, with no warning. Documented in the function's docstring; the real fix (an additional per-(principal, server, tier) aggregate context field, alongside the per-tool one) is a natural, additive follow-up, deferred to keep Decision 1's shipped diff small.
- **Case-sensitive tier matching (`policy.derive_resource`, pre-existing since ADR-005 Decision 3) is now ALSO a velocity-bucket evasion, not just a tier-forbid one — found by adversarial review 2026-08-15.** Tier membership is plain string equality/prefix matching with no case normalization anywhere in resource derivation (only principal emails get `.lower()`'d). GitHub's real API treats `owner/repo` as case-insensitive, so alternating case across identical-repo calls lands each one in a nominally-different tier — previously just an evasion of tier-scoped forbids, now ALSO an evasion of the new tier-scoped velocity count (each casing variant gets its own fresh bucket). Not fixed here — it's Decision 3's code and a blanket `.lower()` isn't obviously correct for every backend (some resource identifiers legitimately are case-sensitive) — needs its own look, priority raised by this amplification.
- **ADR-007 Decision 2 — live-attribute resolver on the resource map, P2 of 4.** Closes "a support engineer can only touch tickets assigned to them" and "a firewall change needs an open change record" with one mechanism: Sentinel calls the backend's own API for a live fact (assignee, open change record) immediately before each Cedar evaluation, fed in as request-scoped entity attributes, never persisted or synced. Must ride the same per-user credential ADR-005's THE RULE already requires for that tool's write path — never a separate, more-privileged "check ownership" credential. Prerequisite for onboarding the next ownership-scoped MCP server (the Snowflake-and-peers catalog item below).
- **ADR-007 Decision 4 — lightweight access reconciliation ("IGA-lite"), P3 of 4.** Monthly job over data Sentinel already logs: birthright entitlements unused in 90 days, confirm/approve grants renewed back-to-back for the same (principal, profile) — standing access hiding behind a timer — and tier-3 shared credentials past a stated rotation interval (new addendum to ADR-005 Decision 10's THE RULE, which names credential tiers but no rotation policy). Output routes through the Mission Control PR gate, since the fix is always a declarative access-matrix edit. Pairs naturally with Phase 8 observability work.
- **ADR-007 Decision 3 — approve-tier grant editing (partial/negotiated grants), P4 of 4.** Let the approving human strike items/verbs from a pending request before granting ("approve 4 of 6 laptops," drop DELETE from a requested [SELECT, DELETE] profile) instead of binary accept/deny. Scoped to the already-rare approve path only (confirm stays one-tap). Fundamentally a console feature — bundle with the console visual-hierarchy pass (below) rather than building it on the current pending-card layout and restyling twice.
- **Sentinel console visual hierarchy — owner review 2026-08-04: "not impressive… doesn't need to be flashy, just needs to make sense."** The interaction design underneath is already right (resolved views, plain-word level labels, provenance chips, advanced-raw-layer-underneath, no-innerHTML XSS discipline) and needs no rework. What's missing is visual priority: the dashboard grid gives "Asking for power" (decisions needed now) the same size/weight as "Active flows" (glance-only), the Access panel — five tabs, forms, history — is squeezed into one card among five equals, count badges look identical whether they mean "3 decisions waiting" or "50 audit rows shown," and the type scale mixes px (v1) and rem (v2 lens iteration) with no consistent hierarchy. Proposed fixes, all CSS/layout only, no new dependency (the vanilla-JS/no-innerHTML security discipline stays): a one-line top-of-page status summary; make the pending-count badge visually urgent (not neutral-gray) when count > 0; split the page into an "act" zone (killbar + pending, full width, dominant) versus a mode-switched "configure" (Access) vs. "observe" (Grants/Flows/Audit) zone instead of one flat auto-fit grid; move Access to its own page/route rather than a card, since it's already structurally a sub-application; unify the type scale on rem. Proposed only — not yet implemented; owner to green-light before building.
- ~~**bootstrap.sh mints the operator kubeconfig from the AMBIENT kubectl context (found by the operator itself during the 6.5 drill — issue [#8](https://github.com/itdan-com/homelab/issues/8)).**~~ DONE 2026-08-01 same day — step 10 now derives the server from the NAMED k3d cluster (`k3d-$CLUSTER_NAME`) and refuses to mint if that cluster is absent from the kubeconfig; named-vs-ambient verified equal on the live host and the refuse-guard verified on a bogus name. Issue #8 closed. Full proof rides the next rebuild's scripted battery.
- **GitHub App cannot comment on or edit issues — `403 Resource not accessible by integration` (found during the 6.5 drill).** It CAN create issues (#3, #6, #8 prove it). Consequence: the escalation path is write-once — the agent can raise but never update or self-correct, so follow-ups are human work (acceptable as design, but make it a *decision*). Revisit when App permissions are next tuned (Phase 7.4 GitHub MCP work).
- **`k6-gateway-key` Secret is rebuild-ephemeral (rediscovered 2026-08-02).** The scale-demo's consumer Secret is an imperative prereq (documented in the script header); it died in the 2026-07-27 rebuild and was re-minted from SOPS for the Phase 6 demo. Either accept (it's a demo rig) or mint it declaratively in bootstrap from `catalog/ai-gateway/secrets.enc.yaml`. Small; decide when bootstrap is next touched.
- **`TOKENS_WARN=72` fires on healthy burst peaks** (twice on 2026-08-02 — both times the agent judged elasticity working as designed). Retune candidate: raise it, or make it per-replica-aware (`rate/replicas > target×1.2`), or accept as a cheap wake-and-check signal. Decide after a few weeks of live tick data.
- ~~**SETUP.md claims the installer trusts the Sentinel CA automatically (2026-08-02):**~~ DONE 2026-08-02 at 7.3.6 — SETUP §1.6 now carries the `ERR_CERT_AUTHORITY_INVALID` troubleshooting block (interop-absent-under-sudo cause, the manual import path, and the browser-restart note).
- **inotify exhaustion is NOT cosmetic — measured 2026-08-04, ten minutes after Loki went live.** Filed on 2026-08-02 as a harmless `systemctl` warning. Loki now shows **460+ `failed to create fsnotify watcher: too many open files` lines per hour across EIGHT namespaces** (kube-system 178, monitoring 149, envoy-gateway-system 36, argocd 32, keda 20, cert-manager 19, authentik 13, chat 13). `fs.inotify.max_user_instances` is at the 128 default and exhausted. **Why it matters:** a process that cannot create a watcher cannot notice a file changing — which is how config reloads and certificate rotations are implemented. Nothing is visibly broken today, and that is exactly the failure profile of something that breaks at the next cert rotation instead. Fix is one sysctl (`fs.inotify.max_user_instances=1024`), persisted in `/etc/sysctl.d/` and added to the host prerequisites so a rebuild does not reintroduce it. **This is also the first thing Phase 8 paid for**: the dismissal was reasonable from a single warning and indefensible against the measurement.
- **The approver grants a window the requester never asked for (noticed 2026-08-02 during the live approve demo).** `CapabilityRequest.window_minutes` records what the person asked for (the elevate page sends the matrix's first offered window, e.g. 30), but the console's Grant buttons are 5.5's fixed **5m / 1h** and `grant_request(ttl_minutes=…)` uses the button, silently ignoring the request. Consequence seen live: an approval looked granted and then the call was refused again a few minutes later because the window was 5 minutes, not the 30 the requester saw. Fix direction: the console card should show and default to the REQUESTED window (and say so), with the approver free to shorten it. Small; do it when the console is next touched, before any second human uses the approve path.
- **MCP tool names: dots on the wire, underscores in the client's display (noticed at the first live session, 2026-08-02).** Claude Code showed `github_create_pull_request` / `echo_say` while the door received `github.create_pull_request` / `echo.say` — proven by the server correctly parsing `echo` as the server half. So display normalization is cosmetic today and the audit identity is intact. **The risk if a future client normalizes on the WIRE:** `partition(".")` yields no leaf, the ladder returns `unmapped-path`, and that client silently gets forbid on everything — safe, but it would look like a policy bug. Decide when a second client type is onboarded: either keep strict (and make the deny reason say "the client sent a tool name without a server prefix") or accept `server__leaf` as an alias, which costs one audit-identity ambiguity. Do NOT add the alias speculatively.
- **Cloud cost shape measured and written up (owner question, 2026-08-04): `docs/cloud-cost-shape.md`.** Platform floor is **2.2 vCPU / 4.9 GiB of requests** — one 4vCPU/16GB node with headroom — plus TWO small always-on VMs (Sentinel's separate trust domain; the Mission Control agent, which must survive the cluster). Key finding: **nothing here needs managed Kubernetes** — `bootstrap.sh` targets k3s, so a cloud VM carries no control-plane fee, which makes k3s-on-VMs ($40-70/mo all-in) roughly half the cost of the default AWS shape whose bill is mostly NAT/ALB/IPv4 idle tax. Rejected from external advice: moving MCP servers to Cloud Run for scale-to-zero — it would remove the sentinel-proxy enforcement point (their NetworkPolicy admits only the proxy; Slack's own auth fails open) to save half a vCPU. Open decision named: does the platform RUN the clusters it manages (what is built, ADR-002's 'domain in, platform out') or CONNECT to customers' existing ones (a different product and security model)?
- **Common enterprise MCP servers as catalog citizens (owner product direction, 2026-08-02).** The stack terraform-deploys with self-hosted MCP servers ALONGSIDE by default — compliance buyers self-host (their own GitHub MCP against GHES, custom in-house servers). After 7.4 (GHES) and 7.5 (Slack) prove the onboarding pattern, survey and add the common set companies actually run — **Snowflake first named** — each as: catalog chart + `servers.yaml` column (tool classes, tiers, resource map) + its own layer-1 credential shape + a matrix column defaulting `none`. The Access GUI's Servers lens is the onboarding surface (GUI v3+ iterates against these real servers, per the 7.2 close).
- **`nomic-embed-text` pulled on the Windows Ollama 2026-08-02, parked.** Not yet a gateway model or catalog citizen — wire it when the vector-DB/RAG work lands (it is the embedding half that stack will want).
- **Scaling/threshold knobs need ONE obvious surface before production hardware changes (owner ask 2026-08-02: "long term it should be super simple to adjust for production scenarios").** Today three layers in three homes, all changeable but not from one place: (1) the autoscaler's brain — `catalog/ai-gateway/values.yaml` (KEDA `threshold: 30` tok/s-per-replica, `maxReplicaCount: 3`; the demo's "90/s" is nowhere written — it is 30×3) — already GitOps-shaped, a values PR away; (2) the watchman's tripwires — env-overridable defaults in `ops/operator/bin/envelope-check.sh` (`NODE_COUNT`, `KEDA_CEILING`, `TOKENS_WARN`, door codes), set per host in the operator env file; (3) the tick guards — `TICK_*` in `launch.sh` / SETUP §1.8. **Known drift coupling:** the envelope's `KEDA_CEILING=3` must match the catalog's `maxReplicaCount=3` by hand, and `TOKENS_WARN=72` is a frozen copy of 0.8×threshold×ceiling — raise the ceiling on new hardware and the tripwires silently lie. Fix direction: derive the derivable (envelope reads the ceiling from the live ScaledObject; warn from the formula) and fold the rest into ADR-002's platform-values contract. Timing: after Phase 8's live tick data, or with the pre-Phase-9 values sweep. Do NOT adjust now (owner). **Product tie-in (owner, same day): company-size sizing profiles** — 150/500/1k/2.5k/5k/10k/15k-person tiers, real k6 validation up to 1000-person cost-gated (500 floor goal), graceful scale-down as half the test — written up as the "Sizing profiles" subsection in `docs/phases/phase-09-cloud.md`; the knobs consolidation is its prerequisite.

- ~~**Chat stack currently BROKEN in-browser: `host.docker.internal` resolution lost.**~~ DONE 2026-07-25 (same session) — root cause: k3s NodeHosts rewrite → fallthrough to stale Windows DNS. Fixed declaratively via `k3d/coredns-custom.yaml` + CoreDNS restart; in-pod resolution verified → `172.19.80.1`. Browser/e2e re-verify pending only on Ollama finishing its Windows update (next session's liveness gate confirms).
- ~~**Delete the stale `host.docker.internal` line from the Windows hosts file**~~ DONE 2026-07-25 — **with an incident**: the first elevated rewrite emptied the entire hosts file (elevated `Get-Content` read nothing, `Set-Content` persisted zero lines; no same-script backup — Claude's error, lesson saved as memory `elevated-writes-snapshot-first`). Restored from known-good content and verified end-to-end: `openwebui.lab.local` → 127.0.0.1 → HTTP 200 from Windows. Stale line and dead `litellm.lab.local` intentionally not restored; evidence copy at `hosts.wiped-2026-07-25.bak`. Owner asked whether any other hand-added entries existed — pending answer; re-add if so.
- **Adopter-docs contract — scheduled work (owner input 2026-07-25).** Rule (now in CLAUDE.md principles): nothing new hardcodes owner-specific values from Phase 2.5 onward. Scheduled: one-command idempotent bootstrap script + `README.md` skeleton in Phase 4; README polish + demo GIF in Phase 4.5; sweep of EXISTING hardcoded values (`/home/bob/homelab-data` in `k3d/devlab-cluster.yaml` + postgres PV hostPath, the gateway IP → bootstrap-detected, GitHub repo URLs) done incrementally as each file is next touched.
- **GPU is shared with gaming on this box.** Observed 2026-07-25: a running Steam game + the loaded model exceeded 12 GB VRAM → driver sysmem fallback → 0.05 tok/s while everything reported healthy. Norm: close GPU-heavy apps before load tests/demos. Later options: a small test model for CI-style checks, VRAM headroom rule, or the cloud phase's dedicated GPU.
- **Mullvad workstation: verify leak-tightness before next use.** Owner stopped both containers 2026-07-25 (side project, out of homelab scope). Its `extras/mullvad-workstation/CHECKLIST.md` verification items (kill-switch on, non-VPN egress dropped, leak tests) are unticked — run those before bringing it back up.
- ~~**Pin the LiteLLM image.**~~ SUPERSEDED 2026-07-25 by ADR-001 — LiteLLM is decommissioned entirely in Phase 2.5; don't spend effort pinning a chart that's being deleted. (Corollary: avoid `helm upgrade litellm` between now and the swap — the rolling tag risk stands until it's gone.)
- **Mac-direct access to OpenWebUI needs cross-machine networking.** Traefik works at the WSL2 host (`curl -H 'Host: openwebui.lab.local' localhost:8080` → 200). For the Mac to reach it: Windows `netsh interface portproxy` (Windows LAN IP:8080 → WSL2 IP:8080) + Mac `/etc/hosts` (`<win-lan-ip> openwebui.lab.local`), OR set `networkingMode=mirrored` in `.wslconfig` (folds into the existing `.wslconfig` backlog item). *(2026-07-26: owner confirmed RDP-from-Mac is the working model — this item is optional comfort, not a gap. It rides with the "LAN teammates" scenario; Phase 5 CA-trust targets the Windows box first.)*
- ~~**The `openwebui` Postgres DB is currently unused.**~~ DONE 2026-07-25 (P2.5.4) — dropped along with `litellm`; postgres now carries no app DBs (`extraDatabases: []`) and stays deployed as the relational store for Phase 5+ (Langfuse; OpenWebUI `DATABASE_URL` migration if horizontal scaling ever needs it).
- **Revisit true non-root for openwebui.** Runs hardened-root because its image assumes root (chowns its data dir at boot); PUID/PGID or a custom image would restore non-root. Do at the PSS/CNI hardening pass before Phase 5.5. *(2026-07-25: litellm half obsolete — decommissioned; the Envoy gateway images run non-root out of the box. One gap to close there instead: the auto-injected extproc container still runs upstream-default resources.)*
- **qwen3.5 burns ~200 reasoning tokens before answering.** Trivial prompts return empty `content` if `max_tokens` is stingy (Ollama's default `num_predict` can truncate mid-`<think>`). Ensure OpenWebUI/clients send generous `max_tokens`; decide whether to expose or suppress `<think>` blocks in the OpenWebUI UX. *(2026-07-25, verified through the new gateway: WORSE than noted — constraint-style prompts ("reply with exactly one word") can loop 2000+ think-tokens to `finish_reason: length` with empty content, and `/no_think` is ignored by qwen3.5:9b. Ollama `/v1` puts think text in a separate `message.reasoning` field which OpenWebUI renders collapsibly, so browser UX is fine. Health-check canaries: plain short questions, `max_tokens ≥ 1500`.)*
- ~~**CNI swap before Phase 5.5.**~~ DONE 2026-07-27 — Cilium 1.19.6 live via the DR rebuild; kube-proxy kept (DOKS parity); Hubble verdict evidence proven. Premise note: k3s's embedded kube-router controller HAD been enforcing basic v1 NetworkPolicy all along (the "Flannel doesn't enforce" line was wrong as shorthand — `--disable-network-policy` removed that controller too, Cilium is now the sole enforcer). CLAUDE.md's Phase-5.5 paragraph still carries the old shorthand — correct it at the next architecture-level CLAUDE.md edit, not mid-session. *(Resolved 2026-07-31: the 2026-07-28 CLAUDE.md rewrite already carries the corrected premise — "enforcement alone was never the whole story".)*
- **WSL2 default-gateway IP is fragile — PARTIALLY MITIGATED 2026-07-25.** The IP has actually been stable since May; the real-world failure was k3s wiping the NodeHosts entry (see activity log). The mapping now lives durably in `k3d/coredns-custom.yaml`; if the IP ever does shift, it's a one-line edit + `kubectl apply` + CoreDNS restart — no cluster recreate. Mirrored networking mode deliberately NOT adopted (container quirks); revisit only if the IP starts actually moving between reboots.
- **busybox `nslookup` is unreliable for cluster-DNS troubleshooting.** Use `nicolaka/netshoot` with `dig` instead. (Caught during 1.5 when an apparent bug turned out to be busybox returning a different upstream answer.)
- ~~**Save the Portainer Agent manifest** so cluster rebuilds are fully declarative.~~ DONE 2026-06-02 — landed at `k3d/portainer-agent.yaml` (not `catalog/portainer-agent/` because Portainer's agent is cluster infrastructure, not a labeled platform workload).
- **Wrap Portainer container into a `compose.yaml`** so its two-network attachment (`bridge` + `k3d-devlab`) is captured declaratively. Currently a manual `docker network connect` after every Portainer container recreate. Do during Phase 2.
- **CPU limits NOT set on k3d nodes** (only memory caps are). When writing Helm charts, set realistic `resources.requests.cpu` — pods that lie about CPU just get throttled, not OOM-killed, but it's still polite.
- ~~**k3d cluster delete is destructive of in-cluster state.**~~ DONE 2026-06-02 in P2.7 — `k3d/devlab-cluster.yaml` now mounts `/home/bob/homelab-data` into every node at `/homelab-data`. `catalog/postgres/` uses an explicit PV with hostPath under that mount. Verified end-to-end: deleted entire cluster, recreated, data still present. Same pattern will apply to OpenWebUI in P2.9 and Langfuse in Phase 5.
- ~~**Parameterize `_template/templates/service.yaml` port name.**~~ DONE 2026-07-25 (Phase 3 item 7) — `service.portName` drives Service port name, targetPort reference, and container port name; postgres can drop its inline override next time that chart is touched.
- ~~**Pin LiteLLM image to a versioned tag.**~~ SUPERSEDED 2026-07-25 — duplicate of the entry above; same ADR-001 resolution.
- ~~**k3d node resource over-reporting.**~~ DONE 2026-07-27 — CPU half closed at the Cilium rebuild: kubelet `system-reserved=cpu=6` (devlab-cluster.yaml) makes each node advertise 2 allocatable vCPU (8 total ≈ physical, post-`.wslconfig`), plus `docker update --cpus=4` burst ceilings in bootstrap (k3d config can't express CPU). Memory half was P2.4.
- ~~**CLAUDE.md vs reality: `.wslconfig` doesn't exist.**~~ DONE 2026-07-25 — created at `C:\Users\Bob\.wslconfig` matching CLAUDE.md exactly (32 GB / 8 processors, NAT networking kept). Takes effect at the next WSL restart; expect `kubectl top nodes` numbers and thread counts to shrink to the documented budget — run the `/resume` liveness gate after that restart.

- ~~**bootstrap.sh: fold in the operator's read-only kubeconfig export.**~~ DONE 2026-07-27 — bootstrap step 10 applies the RBAC and mints `~/.config/homelab-operator/kubeconfig` every run (the k3d API port is random per create, so per-build re-mint is required anyway); verified read=yes / write+secrets=Forbidden. Still open from this item: an interactive `scripts/regen-secrets.sh` walking adopters through regenerating all five SOPS files (SETUP.md §1.1 is manual today).
- **Monitoring state is rebuild-ephemeral (accepted 2026-07-27).** Grafana + Prometheus use k3s `local-path` PVCs inside node containers — a cluster rebuild wipes metrics history and any hand-made Grafana changes (dashboards/datasources/OIDC are provisioned from the chart, so the platform re-converges identical). Revisit only if history starts mattering: move both onto hostPath PVs under `/homelab-data` the way postgres/openwebui already do.
- **k3s image pin is aging (noticed at the 2026-07-27 rebuild).** Still `v1.31.5-k3s1` (May). Deliberately NOT bumped during the CNI swap (one variable at a time). Bump is its own small session: check Cilium 1.19 + kubectl ±1-minor compat, update `k3d/devlab-cluster.yaml`, rebuild via bootstrap, run the battery (gate + dance + smoke are all scripted now — the rebuild is cheap).
- ~~**Hubble UI when Sentinel work starts.**~~ DONE 2026-07-27 at 5.5.4 — `k3d/cilium-values.yaml` `hubble.ui.enabled` + live helm upgrade; `kubectl -n kube-system port-forward svc/hubble-ui 12000:80`. Demo-asset capture still pending (owner screenshot of a DROPPED flow for the README when convenient).
- **SQLite WAL sidecars were committed to the public repo (2026-07-27, caught same session) — history decision is the owner's.** Enabling WAL at 5.5.5 created `sentinel-dev.db-wal`/`-shm`, which `.gitignore`'s `*.db` did not match, so `git add -A sentinel` committed them in `3c261b1`. A WAL file holds pages of the database. **What actually leaked:** two plaintext capability tokens for `echo.say` on flow `final-check` — verified **expired**, scope-locked to one tool and one flow, and usable only against a broker on this host that additionally demands an mTLS client cert from Sentinel's private CA. No TOTP seeds, no session tokens (never stored in plaintext), no CA keys. **Done:** `*.db-wal`/`*.db-shm`/`*.db-journal` added to `sentinel/.gitignore` with the reasoning, files untracked. **Open — owner's call:** whether to purge `3c261b1` from history. Leaving it is defensible (dead tokens, unusable without a private cert from behind a home NAT); rewriting means a force-push over `protect-main` and breaks every existing clone including the operator bot's. **The transferable lesson is the point:** an ignore rule that names a file extension does not cover the file's satellites, and turning on a database feature can silently create new committable artifacts — audit `git status` after any storage-engine change.
- **Phase 7.1 candidates (moved 2026-07-31; recorded 2026-07-27 as "Phase 6 candidates", owner ideas): XAA + Cedar make all three layers real.** Written up in `docs/phases/phase-07-airlock.md`. **XAA** (Cross App Access / ID-JAG, in the MCP spec since 2025-11-25 as SEP-990) is layer 1 done properly — short-lived scoped delegation instead of a static upstream credential; it does NOT conflict with Sentinel (it answers "may this app reach that resource at all", Sentinel answers "may this task use this tool now"), but it makes the IdP the control plane, so it needs an IdP the agent cannot PR — not in-cluster Authentik. **Cedar** is layer 3: the dominant MCP policy language, and the standard architecture is ours (gateway evaluates policy) — but we already HAVE the gateway, so the move is replacing `check_capability`'s conditionals with a Cedar evaluation in the broker rather than adding a sidecar hop. Same constraint: policy governing the agent must live where the agent can't edit it — host-side, or a second repo the operator's App isn't installed on. **Why it matters:** Cedar is plausibly the answer to what 5.5.8 is about to measure (if one MCP session needs 4-5 approvals, the one-grant-per-call model is unusable and humans rubber-stamp). Sequencing agreed: measure first, then one ADR covering both.
- **Phase 9 (cloud; renumbered 2026-07-31) blockers found 2026-07-27 — "the catalog deploys unchanged" is not true yet, and it's k3s's fault, not the cloud's; now also in `docs/phases/phase-09-cloud.md`.** Owner challenged the DigitalOcean-specific language in ADR-004 (fair — corrected to a provider-neutral shape with an AWS/GCP/Azure/DO mapping table). Auditing the claim surfaced three real gaps, all provider-INDEPENDENT: (1) **every door is a Traefik `IngressRoute` CRD in eleven charts, and Traefik is not in `catalog/`** — it comes free with k3s, so on EKS/DOKS/GKE/AKS alike those resources fail with "no matches for kind IngressRoute"; fix is either a Traefik catalog chart or migrating doors to Gateway API `HTTPRoute` (better — Envoy Gateway is already running, so it's one gateway implementation instead of two, but a bigger change); (2) **`storageClassName: local-path`** is k3s-only and must become the platform value ADR-002 already names; (3) **ServiceLB/host-port reasoning inverts in cloud** — `EnvoyProxy` is `ClusterIP` locally *because* k3s ServiceLB fights Traefik for 80/443, which is a local workaround baked in as a constant. Lesson to carry: anything k3s hands us for free is a hidden dependency, and hidden dependencies are what make an "it's just Kubernetes" claim false at the worst moment. **Also recorded in ADR-004:** the console should NOT be public in cloud — an SSH tunnel or mesh VPN keeps it loopback-bound on the VM (refuse-to-start guard satisfied unchanged, no cert, no DNS record, WebAuthn still works because the browser sees `localhost`); the public path is only needed for the roadmap's phone-approval feature.
- **ADR-004 debts, ranked (do NOT lose these).** From the 2026-07-27 adversarial review, deliberately not fixed at 5.5.5 — full detail in `docs/adr/ADR-004-sentinel-trust-domain-in-cloud.md`: (1) **no tenant scoping** — no owner column on flows/grants/requests and `flow_id` is client-chosen + globally unique, so two users' `flow-1` collide; cheap now, an unattributable backfill once the audit log has history — **fix before Phase 6 writes real flows**; (2) **one client cert for the whole Envoy fleet** covering both ask-for-power and enforce-power (claim nonce removed the worst consequence; per-caller certs + CN in every audit row is the real fix, and it's a chart-topology change); (3) **audit log is a mutable local table** — no retention/rotation/export/integrity; a `prev_hash` chain is ~10 lines now; (4) **revocation is all-or-nothing** — only the global kill switch exists, which makes operators hesitate to use it; add per-grant and per-flow revoke; (5) **enforcement is opt-in by the guarded workload** — one values flip removes the gate and ArgoCD converges happily; wants a namespace default + admission policy keyed on `exposes-mcp: "true"`; (6) **no egress policy on fronted workloads** — ingress proves who can reach a hostile MCP server, never where it can reach.
- **Sentinel needs a real-HTTP + browser smoke at 5.5.8.** The console was verified by curl and by pytest, not by a human in a browser — the page renders correctly by construction (no framework, `textContent` only) but nobody has *looked* at it. Owner: open `http://127.0.0.1:8400/` on the Windows box while a request is pending; it doubles as a demo-asset screenshot (with the Hubble UI DROPPED view).
- **Upstream report candidate: ai-gateway-helm webhook certs regenerate every render.** Bites any ArgoCD/Flux user of the chart (v1.0.0): cert Secret rotates per sync, controller-patched caBundle strands, `failurePolicy: Fail` then blocks all EG-provisioned pod creation. Our fix (ApplicationSet ignoreDifferences + RespectIgnoreDifferences) is generic; consider filing upstream (envoyproxy/ai-gateway) with the repro — good open-source citizenship + a visibility win.
- **Real-HTTP Sentinel smoke test (nice-to-have).** The prior Fable session started `sentinel/smoke.sh` (boots both uvicorn listeners on a throwaway DB, curls the full request→grant→poll→check→kill→release loop over real HTTP) but it was interrupted by the mid-session model swap and removed as a broken stub. The committed `tests/test_broker_flow.py` (in-process, 20 asserts) + a manual curl walkthrough already cover the behavior; a committed real-HTTP version is complementary. Redo cleanly at 5.5.8 (end-to-end smoke) if wanted.
- **Rebuild backups retention.** `~/homelab-data-backups/pre-cilium-rebuild-20260727.{sql,tar.gz,state.txt}` (~835M) went unused — hostPath survival worked. Keep until the next successful rebuild, then rotate (owner call).
- **HitmanPro.Alert malware protection is currently DISABLED** (owner, 2026-07-25, to unblock the false-positive-flagged Ollama 0.32.4). Re-enable within a few days: exclusions for both Ollama exes are in place. (FP report to Sophos: owner opted to skip — the hash should age into reputation on its own.) After re-enabling, relaunch Ollama as the test; the `/resume` liveness gate catches a re-block. Expect a possible re-trip on the NEXT Ollama update (new hash, zero reputation) until Sophos fixes the verdict cloud-side. **Owner-managed — per owner 2026-07-26, do not raise in sessions;** the liveness gate surfaces any re-block on its own.
- **Chart update runbook with easy rollback (owner ask 2026-07-25).** OpenWebUI's UI shows an update banner — in-app update is NOT the path; the chart is. Document in the operator cheatsheet: (1) snapshot the app's hostPath data dir first (`tar` of `~/homelab-data/openwebui-openwebui` — app-level DB migrations are the real rollback risk; a snapshot makes them reversible), (2) bump `appVersion` in Chart.yaml, (3) `helm upgrade`, (4) on regression `helm rollback` + restore the snapshot if migrations ran. Phase 4 turns steps 2–3 into a reviewed PR + ArgoCD sync and rollback into `git revert`.
- **Multi-brand agents behind the same gate (owner idea 2026-07-25).** Nothing "controls" — everything proposes; the gate is brand-agnostic. Slot a second-vendor agent (e.g. Codex) as (a) an independent PR reviewer attached to Slack approvals — cross-vendor review de-correlates blind spots — and (b) a fallback proposer during Anthropic outages, with its OWN GitHub identity for audit attribution. Consider post-Phase 4.5; keep it optional in the open-source story (don't force adopters to hold two vendors' keys).
- **Tiered model routing: local model for bulk, paid model for complex (owner idea 2026-07-25).** All primitives exist as of Phase 2.5: gateway model aliases as GitOps policy (`platform-routine` → local Ollama backend; `platform-hard` → cloud backend + BackendSecurityPolicy), guardrails = lint/policy-checks/CI + tier gates + per-flow token budgets, PRs labeled `authored-by:` feeding a per-tier approval-rate dashboard (Phase 7). Escalate on validation failure or self-reported low confidence. Gate a local model's admission on a replay-eval (past approved PRs as the test set). Also: a chunk of "bulk" should become deterministic runbooks with NO model in the loop. Post-Phase 6.
- **Approval-gate hardening (operator finding, 2026-07-26).** The negative test held 3/3 (charter decline; 422 self-approve; 405 review-less merge — evidence in phase-04-5 notes), but the merge attempt CLEARED token permissions (`contents=write` accepted) and was stopped only by `protect-main` — the single configurable control between the App and `main` (App perms have no per-branch granularity; `itdan-com` is a User account so no org-level duplicate ruleset; bypass list verified = repo-admin only). Follow-ups: (1) Phase 6 item 6.4 — the admin-bypass decision (straight removal deadlocks the builder's own push path; options in the phase doc); (2) Phase 8 — alert on ruleset-change/deletion events (`repository_ruleset` webhook → `#claude-alerts`) so a silent gate change pages; (3) evidence issue filed by the operator: [#3](https://github.com/itdan-com/homelab/issues/3) (builder response with admin-verified bypass-list check posted same day).
- **Model serving joins the catalog (owner question 2026-07-27; Phase 9-adjacent).** Ollama-on-Windows stays the RIGHT local backend (the GPU is on Windows; k3d GPU passthrough = fragility for zero gain) and is already a named local-only artifact behind the gateway. The scale path is NOT better Windows automation (`winget install Ollama.Ollama` exists — worth a SETUP.md line for host reproducibility someday): it is a `catalog/` serving chart for cloud — Ollama-in-cluster for continuity or **vLLM** for production throughput — with models DECLARED in values (pull Job/initContainer), one Deployment per model on GPU node pools, KEDA per-backend on the Phase-3 token signal, cluster autoscaler adding GPU nodes. The gateway already isolates every consumer from this choice (model name → backend mapping in values), so "install any model" = one values entry + one backend release. Lift ≈ one session for the chart; schedule at Phase 9 (optionally a pre-9 CPU-tiny-model dry run).
- **authentik expression deprecation (noticed 2026-07-27).** Our `openwebui-roles` scope-mapping expression uses `user.ak_groups` — authentik warns it's deprecated (fired on blueprint apply). Works on pinned 2026.5.6; swap to the replacement accessor at the next authentik version bump (check release notes).
- **OpenWebUI duplicate model listing (cosmetic, check next UI visit).** With bypass on, `/api/models` returns `qwen3.5:9b` twice — the connection model + the DB workspace entry someone created via Admin→Models. If the picker shows dupes, delete the workspace copy (Admin → Settings → Models); the connection one is canonical.
- **Catalog-contract v2 (operator issue [#5](https://github.com/itdan-com/homelab/issues/5), accepted 2026-07-26).** Order: (B) amend `catalog/README.md` — only `_helpers.tpl` is the byte-identical contract; `deployment.yaml`/`service.yaml` are a starting skeleton charts may outgrow; (A) minimal `with`-guarded `args`/`command`/`env`/`envFrom` in `_template/templates/deployment.yaml` (probes stay chart-local). Then the adjacent backports: openwebui + postgres `service.yaml` (and openwebui's hardcoded port name) with byte-identical `helm template` output as the PR-body proof. `scripts/template-sync.sh` only if A grows. **DONE 2026-07-26 same day** — landed with the full proof set: 4 rendered outputs byte-identical before/after, echo dep + both service files file-identical to template, helpers asserted ×3, 4 charts lint clean, zero rollouts. Issue #5 closed.

---

- **ADR-002: cloud-parity contract ("domain in, platform out") — ACCEPTED + written 2026-07-26** (`docs/adr/ADR-002-cloud-parity-contract.md`; owner: "this entire thing deployed from terraform might be the move... choose my domain in terraform and bam"). Consequences applied same day: phase-05 B4 blueprints now REQUIRED, A5 scoped by the exposure policy, CLAUDE.md Phase-8 paragraph reframed. Originally proposed as: The product bar: deploy from GitHub to a cloud provider with a domain and get TLS/SSO/OAuth working — no friction, multi-user, scalable. Deltas to write up and apply: (1) a **global platform values convention** — `domain` (lab.local → yourdomain.com), `issuer` (lab-local-ca → letsencrypt), external port (8443 → 443), storage class (hostPath → provider PVs) — charts are already per-service parameterized; lift to one root-level contract so cloud = set 3–4 values. (2) **Phase 8 reframed**: not a throwaway drill — the deliverable is `terraform apply` + domain → working public platform (LE certs, SSO up headless); the destroy is cost hygiene after proof, and "keep it up" becomes a supported choice. (3) **Authentik config MUST be config-as-code** (blueprints) so SSO assembles with zero clicks in cloud — hardens phase-05 B4 from "prefer" to "require". (4) **DNS story per environment**: hosts-file locally, external-dns or documented A-record in cloud. (5) **Multi-writer scale** (Phase 6 note): GitHub merge queue + required status checks (lint/render) once >1 proposer exists; per-service chart dirs already minimize conflict surface; ArgoCD converges to main regardless of merge order. (6) **Multi-user**: SSO (Phase 5) is the unlock; per-user gateway keys/budgets as a follow-on.

## Blocked / waiting

_Things stuck on external dependencies (cloud credit, approvals, vendor responses, etc.)._

- (none)

---

## Token-economy reminders for working sessions

- One phase per session when possible. Don't drag the whole plan through every conversation.
- Sonnet for executing checklist items; Opus for architectural decisions; Haiku for trivial edits.
- Use `Explore` subagent for any codebase search with > 3 likely targets.
- Don't churn `CLAUDE.md` mid-session — it lives in the prompt cache; stable file = warm cache.
- Use `TaskCreate` for within-session step tracking; the markdown files here are the cross-session source of truth.
