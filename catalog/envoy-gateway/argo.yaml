# GitOps self-declaration: presence of this file onboards the chart to
# ArgoCD via the catalog ApplicationSet. name MUST equal the existing
# helm release name so adopted resources line up.
name: envoy-gateway
namespace: envoy-gateway-system
secrets: "false"
