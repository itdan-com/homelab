# GitOps self-declaration (see catalog/argocd ApplicationSet).
name: github-mcp
namespace: mcp-servers
# Carries a credential: the ApplicationSet decrypts secrets.enc.yaml.
secrets: "false"   # holds NO credential — Sentinel injects it per request (7.4)
